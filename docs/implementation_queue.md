# Implementation queue

Status: execution order for the next research cycle, 23 September 2026. This is a navigation and handoff document; detailed design, tests, and stop rules remain in the linked work packages. See the [next execution plan](next-execution-plan.md) for the completed Q09 closeout and ordered sequence through the combined frontier. Re-read repository status before starting. No training run, data download, paid service, or public claim is authorized by this queue.

## The objective

Find out whether a small model can retain more useful capability per measured memory and compute than a fair conventional Transformer. Every stage should eliminate a weak idea cheaply. A correct but slow reference implementation is useful; an attractive architecture diagram is not evidence. Stop branches as soon as a decisive failure is established.

## Immediate queue: remove blockers, then run one comparison

| Order | Package | Best fit | Dependency | Deliverable and pass gate |
|---|---|---|---|---|
| Q00 | Reconcile the checkout | Luna | None | Read `AGENTS.md`, inspect the current diff, run the repository test/style commands, rebuild the wheel, and update stale status/count/hash text. Resolve every failure; do not train. Record the exact validation commands and outcomes. |
| Q01 | Freeze this revision | Luna | Q00 | Review the combined diff for scope, verify no generated data/checkpoints/secrets are tracked, and make one local commit containing the accepted infrastructure/protocol changes. Every future control and candidate must use a clean pinned revision. Do not push as part of this package. |
| Q02 | Rebuild the bounded TinyStories split | Luna | Q01; pinned raw prefix available | Use the documented source revision and exact byte range; verify raw-prefix hash, source sidecar, transformation, complete-story count, and newly prepared schema-3 manifest. Keep old files/runs unchanged. Gate: regenerated split hashes match the new manifest and `content_origin=synthetic`; otherwise stop and diagnose. |
| Q03 | Freeze the comparison protocol | Luna | Q00 | Verify current comparator/schema against `AGENTS.md` and E02. Add only any missing adversarial tests: absent/null provenance, duplicate or invalid seeds, non-finite metrics, unsupported compute, dirty/mismatched source, and incomplete runs. Gate: no caller option weakens the three-paired-seed claim floor. |
| Q04 | Close the minimum evaluation and compute gates | Sol | Q00 | Finish only the missing E05/E06 behavior needed by the first study. Confirm exact scored-token boundaries, byte coverage, module-aware cost support, and explicit unavailable states. Unknown module cost must disable compute-equivalence claims. Gate: hand-calculated tiny models agree with serialized accounting. |
| Q05 | Make the pilot eligible | Luna | Q01-Q04 | Create immutable MHA/GQA config copies under `configs/m2/` that point to the new schema-3 split while preserving every other preregistered field; hash them and create the dry-run plan. Verify tokenizer/model/config hashes, environment, clean revision and planned compute. Gate: a fresh process loads the data and plan without model allocation; no unknown provenance remains. |
| Q06 | Run a bounded GQA smoke pilot | Luna | Q05; S01 tests pass | **Complete:** seed 17, 32 updates per arm, pause/resume at update 16, evaluation and profiles. This is stability/resource plumbing only, not a capability claim. |
| Q06b | Run the one-seed GQA compute screen | Luna | Q06 passes | **Complete:** seed 17, 51/54 updates and 1.352T estimated FLOPs total. Keep scores descriptive; proceed only because both runs completed and the screen stayed within the predeclared practical loss budget. Do not claim improvement. |
| Q07a | Audit the first full GQA confirmation | Sol | Q06b | **Complete, ineligible.** All six runs finished, but `compare-seeds` failed the existing same-recipe gate because equal 100-step warmups were 6.055% different as fractions of each arm's total schedule. Preserve all artifacts and disclose descriptive outcomes; do not weaken the gate or reuse these runs in a claim. |
| Q07b | Repeat the corrected paired GQA confirmation | Sol | Q07a; amended configs/spec committed; clean revision | **Complete:** six runs on clean revision `73bde6e` passed all comparator eligibility checks at 81.666T estimated FLOPs. Mean paired NLL delta was -0.02307 nats/token (SD 0.02439); actual cache bytes were halved. CPU cached decode was about 8.5% slower, so this is a synthetic-domain quality/cache tradeoff, not a broad or statistically conclusive capability win. Full per-seed record is in `EXPERIMENTS.md`. |
| Q08 | Close state-accounting E08/E09 gaps | Sol | Q00; no architecture run required | **CPU sampled scope measured:** profile schema 4 uses isolated CPU workers for prefill/decode and exact storage-interval union for allocated/active sequence state. Q08-CPU-STEP-002 completed one update in 0.151018 s and collected 11 current RSS/private-memory samples with observed gaps of 9.457–17.097 ms. This resolves the earlier boundary-only sampling limitation for this workload but remains a sampled maximum. Sweep plans accept a cumulative runtime cap; graceful and interrupted resumes are covered. Full E08 remains open for CUDA validation, unavailable on this host. |
| Q09 | Export portable evidence | Luna | Q07 or any completed run | **Portable bundle and bounded fixture reproductions complete.** The tracked six-run M2-GQA-002 bundle sanitizes paths/content, hashes omitted artifacts, and independently recomputes comparison eligibility; it verifies from a detached clean checkout. Two fresh deterministic smoke runs at `74bbe3d28f3f80a00fac3a03aac0108036cf4cdd` and a run from a separate clean clone at `790a83ac5c60b26ed0bd983d267ae28374488bad` reproduce the recorded fixture checkpoint and metrics. These checks validate environment/checkout plumbing only; they do not rerun M2-GQA-002. An independent M2 comparison rerun remains a separate later evidence question, not a capability claim or a prerequisite for corpus preparation. |
| Q10 | Freeze the next corpus decision | Luna | Q00-Q05 | **Q10-PREP-002 complete at `c57f186`.** The restricted-prose rights gate passed 21/21; two extractions and schema-4 preparations reproduced a 593,275-byte corpus (`1f237ede…1b8a7`), 17/2/2 split with no duplicate groups, and train-only 512-token byte-BPE artifact (`0570f753…6bb33`). Validation/test PMCID.version assignments and all hashes are recorded in the [result inventory](q10i_pmc_preparation_result.json). Corpus and tokenizer files remain ignored under `data/`. Q11.a task/scorer and contamination gates, and Q11.b's fresh conventional GQA baseline, are complete. Sequence correctness work is next; sequence-module training remains gated on those invariants and a fresh matched comparison. |
| Q11.a | Validate versioned tasks, scorer and contamination contract | Sol | Q10-PREP-002 complete | **Complete:** scorer hand-checks, byte normalization and deterministic audit pass. Two Q10 A/B scans were byte-identical; four tasks had zero full-identity and zero shared 13-token overlaps across all 21 frozen train/validation/test documents. Hash-only result: [`q11_task_contamination_result_v1.json`](q11_task_contamination_result_v1.json). No model metrics or training were used. |
| Q11.b | Produce fresh conventional GQA baseline results | Sol | Q11.a passes; budget and configs committed; clean revision | **Complete:** seeds 17/42/123 each consumed 131,072 tokens (256 updates) at clean revision `549ff8d`; all three completed with no failures under 2,164,663,517,184 estimated FLOPs total. Mean validation score was 3.69769 bits/byte (sample SD 0.01007); the toy task scored 0/12 exact matches. CPU prefill/decode latency, isolated phase RSS, cache bytes and all per-example outputs are recorded in [`q11_baseline_result_v1.json`](q11_baseline_result_v1.json) and the verified portable evidence bundle [`results/q11-baseline-001`](../results/q11-baseline-001/manifest.json). Q10 test model metrics remain sealed. |

### Current package status

**Q00 complete (23 September 2026):** the initial 83-test suite, Ruff checks, local links, `git diff --check`, and wheel build passed; wheel SHA-256 `130fb5d67d850fb667f17cbdc336222edf20e9380c6e4fc9297677835ef83b84`. **Q03 is also complete:** seeded comparison schema 3 validates saved configs, tokenizer/data artifacts and source hashes, then recomputes actual training cost from consumed tokens. Scope-specific synthetic claims are separate from fixture results and human-corpus claims. Non-finite loss/gradient stops and refusal to overwrite existing prepared corpora are regression-tested. **Q01 complete:** the reviewed infrastructure update and freeze record are local commits `a0d09fa` and `6ce18c5`; they are included in public `main`, whose pushed tip is `eef4f6f`. **Q02 complete:** the pinned prefix hash is `3232df12d6bdd8f83071f0d8196ad10128624168e228aaf9a23f6a1f8431ee8a`; reprocessing recovers 23,310 complete stories and discards 117 trailing partial bytes. The schema-3 split records `content_origin=synthetic`, preserves the M1 split hashes, and has manifest SHA-256 `5bac1fc6d9ab7af5ed4c9a61d3a5ccbd2426faebb75acf2b605b013e146841ac`. The old data is unchanged; corpus files remain ignored locally. **Q04 complete for the first GQA study:** exact-once target scoring, UTF-8 byte coverage, hand-derived tiny-model FLOPs, unknown-module unavailable states, and compare-time recomputation from consumed tokens are covered by 49 evaluation/compute/comparator tests; E05's broader task/evaluation freeze remains open. **Q05 complete:** the MHA/GQA configs and UTF-8 byte-tokenizer artifact are frozen; a clean-revision three-seed plan verifies six jobs, -0.00363% per-seed compute delta and 81,666,247,753,728 total estimated FLOPs under an 85T cap. **Q06 and Q06b complete:** smoke/resume and one-seed compute screen passed plumbing gates; no capability claim. **Q07a completed but is ineligible:** all six full-budget runs finished at revision `d12da08`, but equal 100-step warmups differed by 6.055% as schedule fractions; no paired claim is allowed. **Q07b complete:** corrected 100/106-step warmups produced a 0.418% relative fraction gap; all six jobs on clean revision `73bde6e` passed synthetic-domain comparison eligibility at 81.666T estimated FLOPs. Mean paired NLL delta was -0.02307 (sample SD 0.02439); GQA halved cache bytes but decoded about 8.5% slower on this CPU. This clears the preregistered point-estimate screen, not a significance test or human-capability claim. **Q08 scoped CPU/state/cap work complete; E08 remains partial:** the sampled CPU optimizer-step probe captured 11 current-memory readings over 0.151018 s, with 9.457–17.097 ms gaps. The maximum remains sampled; this is not an exact transient peak. CUDA behavior cannot be validated here. Isolated CPU phase peaks, shared/opaque state accounting and cumulative runtime after graceful or interrupted resumes are covered by the recorded suite. **S02 state contract complete:** profile schema 5 and accounting version 2 preserve same-checkpoint logits/cache bytes; see [`s02_state_result_v1.json`](s02_state_result_v1.json). E08 and E09 retain the open items described in `docs/roadmap-measurement.md`.

Q07b is closed with a qualified synthetic-domain result. Q08 scoped CPU state/memory and sweep-cap work is complete; Q08-CPU-STEP-002 collected 11 sampled CPU readings during its 0.151018 s optimizer step, while CUDA is unavailable and an exact transient CPU peak is unresolved. Q09's tracked M2-GQA-002 bundle verifies from a detached clean checkout; same-environment and separate-clean-clone fixture runs reproduced the smoke model state/checkpoint and metrics, not the M2 comparison. An independent M2 rerun remains an evidence limitation. **Q10 source and preparation status:** OpenStax was withdrawn before acquisition after its publisher's LLM-training restriction was found. The Wikipedia source screen remains a failed historical record with zero of 24 pages cleared. Q10-SOURCE-002 (5/24) and Q10-SOURCE-003 (zero IDs after query syntax failure) remain closed historical attempts. Q10-SOURCE-004 passed its metadata gate: 21/48 candidates, 3,153,456 expected XML bytes. Q10-SOURCE-005 retrieved and verified all 21 authorized XML objects. Q10-SOURCE-006's explicit-href-only gate remains a valid historical failure: 13 permitted hrefs and eight ALI-only; its minimum was unattainable. Under the separately preregistered SOURCE-007 rule, all 21 passed the restricted abstract/main-body rights review with no unresolved included-prose rights notice. Q10-PREP-002 then produced identical A/B extraction, split and tokenizer hashes; its detailed result is in `docs/q10i_pmc_preparation_result.json`. Raw XML, corpus text, splits and tokenizer artifacts remain ignored under `data/`. Q11.a and Q11.b are complete; the three-seed conventional GQA control is recorded in `docs/q11_baseline_result_v1.json`. S02 passed without training; S03 dense local-attention correctness is next. New learning comparisons must use the frozen corpus/evaluation recipe and fresh matched controls. Any code or protocol fix during a pinned run requires preserving that attempt, committing a new revision and restarting both arms.

## Architecture sequence: each stage is conditional

| Order | Package | Best fit | Source of full specification | Decision gate |
|---|---|---|---|---|
| A01 | Explicit cache/state contract | Sol | [sequence S02](roadmap-sequence.md#s02--make-sequence-state-explicit-and-inspectable) | **Complete:** absolute positions, key offsets, chunk/full/token parity and state bytes pass before bounded caches are added. No training. |
| A02 | Dense local-attention reference | Sol | [sequence S03](roadmap-sequence.md#s03--implement-a-dense-local-attention-correctness-reference) | Reference outputs/gradients match the slow oracle at boundaries; cache stays bounded. Label its prefill compute dense. |
| A03 | Local-attention learning pilot | Luna | [sequence S04](roadmap-sequence.md#s04--test-the-local-attention-capability-tradeoff-cheaply) | One seed at <=5% planned compute; stop on instability or an already-dominated resource/quality point. Confirm only if signal warrants three pairs. |
| A04 | Local prefill backend | Sol | [sequence S05](roadmap-sequence.md#s05--add-one-implementation-that-actually-performs-local-prefill-work) | Implement only after profiling proves the reference's actual bottleneck; keep reference and numerical parity. Kill if workload latency/memory does not improve. |
| A05 | Normalized causal linear-attention oracle | Sol | [sequence S06](roadmap-sequence.md#s06--implement-normalized-causal-linear-attention-as-a-reference) | Stable finite gradients, no future leakage, bounded persistent state, and chunk parity. Stop before kernels if the reference cannot learn the toy dependency. |
| A06 | Optimize the surviving cheap block | Sol | [sequence S07](roadmap-sequence.md#s07--decide-whether-a-recurrent-implementation-deserves-optimization) | Only optimize a module with a measured tradeoff; optimized path matches reference and actual peak storage/work are reported. |
| A07 | One fixed hybrid schedule | Luna | [sequence S08](roadmap-sequence.md#s08--test-one-hybrid-schedule) | Compare pure attention, pure cheap block, and one fixed hybrid; no schedule search. Three paired seeds only after one-seed stability. |
| A08 | Small preregistered schedule/context extension | Luna | [sequence S09-S10](roadmap-sequence.md#s09--separate-window-size-from-attention-placement) | At most four validation configs; include search compute and fresh matched controls. Stop at budget cap; held-out set stays sealed. |
| A09 | Sequence result freeze | Luna | [sequence S11](roadmap-sequence.md#s11--freeze-the-sequence-research-checkpoint) | Portable artifacts, technical report, and an independent rerun reproduce the selected result; otherwise label unresolved. |

## Compression, teacher use, and inference: do not combine early

| Order | Package | Best fit | Source | Decision gate |
|---|---|---|---|---|
| C01 | Quantization format and memory ledger | Luna | [compression C01](roadmap-compression.md#c01--quantization-specification-and-accounting) | Define exact weight grouping, rounding, scales, zero handling, exclusions, and real resident-memory accounting. |
| C02 | Fake-quant linear reference | Sol | [compression C02](roadmap-compression.md#c02--fake-quantized-linear-reference) | Forward/gradient/serialization invariants pass for supported bit widths; floating master weights are never counted as low-bit storage. |
| C03 | Low-bit stability screen | Luna | [compression C03](roadmap-compression.md#c03--low-bit-training-falsification-matrix) | Screen few preregistered formats at a small budget; promote at most one. Stop formats with unstable training or no plausible benefit. |
| C04 | Quantization schedule ablation | Luna | [compression C04](roadmap-compression.md#c04--quantization-schedule-ablation) | One variable per schedule; compare equal compute and include all optimizer/master-state costs. |
| C05 | Packed checkpoint round trip | Sol | [compression C05](roadmap-compression.md#c05--packed-checkpoint-format-and-round-trip) | Packed bytes, metadata, scales and load path are versioned; exact dequantized-reference tolerance and corruption rejection pass. |
| C06 | Packed CPU execution reference | Sol | [compression C06](roadmap-compression.md#c06--cpu-packed-weight-execution-reference) | Establish end-to-end memory and latency against the same float checkpoint. No speed claim from file size. |
| C07 | Native kernel feasibility | Sol | [compression C07](roadmap-compression.md#c07--native-kernel-feasibility-gate) | Build only if the CPU/reference profile identifies matrix unpack/compute as a material bottleneck. Keep fallback and backend identity explicit. |
| D01-D02 | Teacher ledger and ordinary SFT control | Luna | [compression D01-D02](roadmap-compression.md#d01--teacher-provenance-and-cost-ledger) | Pin teacher/model/data/revision; charge acquisition/generation and student costs; establish SFT before distillation. |
| D03-D04 | Logit distillation and exact target cache | Sol | [compression D03-D04](roadmap-compression.md#d03--full-logit-distillation-reference) | Correct temperature/mask/KL math and cache provenance; compare with SFT at equal student compute and disclose teacher-inclusive cost. |
| D05-D06 | Representation then validated task targets | Sol | [compression D05-D06](roadmap-compression.md#d05--representation-distillation) | Run only if logit distillation survives. Separate representation objective from trajectory targets; validator correctness and contamination checks required. |
| D07 | Quantization-distillation interaction | Luna | [compression D07](roadmap-compression.md#d07--quantization-aware-distillation-interaction) | Only after each intervention independently survives; preregister all four factorial cells and interaction, not best corners. |
| I01 | Inference workload lock | Luna | [compression I01](roadmap-compression.md#i01--deterministic-inference-budget-baselines) | Freeze prompt/output shapes, batch, timing, warmup and cache protocol; establish repeat variance before optimizing. |
| I02-I03 | Speculation correctness | Sol | [compression I02-I03](roadmap-compression.md#i02--greedy-speculative-decoding-correctness) | Greedy exact-token equality first; then sampling distribution correctness against tiny analytical cases. Count draft model/cache/work and latency. |
| I04-I05 | Adaptive budget allocation | Sol | [compression I04-I05](roadmap-compression.md#i04--adaptive-computation-feasibility-experiment) | Test static two-model routing before learned early exit. Must beat fixed allocation including router, worst-case resident memory and tail latency. |
| X01 | Combined frontier replication | Luna | [compression X01](roadmap-compression.md#x01--combined-frontier-replication) | Combine no more than two individually surviving changes; fresh conventional controls, all costs, uncertainty, portable artifacts, independent rerun. A negative result is an acceptable endpoint. |

## Non-negotiable rules for every handoff

- One package, one owner, one stated acceptance gate. Luna is the default for docs, schemas, test additions and small bounded fixes; Sol is the default for multi-file model/runtime changes. Either model must stop if the requested package depends on an unfinished gate.
- Start by checking the current tree and reading `AGENTS.md`, `RESEARCH.md`, `EXPERIMENTS.md`, measurement protocol, and the chosen package. This queue may be stale by the time work starts.
- Do not download data, install large dependencies, launch paid compute, or push a repository unless separately requested. Preserve existing user changes and historical outputs.
- For code: add a failure-oriented test, run focused tests, then all required tests/Ruff checks. For protocols: verify paths, links, and CLI options against the code.
- Before research training, preregister all eight fields, pin and commit the code/config/data protocol, ensure clean Git state, and run a smoke job. A pilot is not a claim. A claim needs at least three paired seeds, eligible compute/provenance, a declared practical effect, and complete failure accounting.
- Never optimize a module merely because it is novel. First show that its accurate reference addresses a bottleneck relevant to the target device/workload. Never combine sequence, quantization, distillation and inference interventions until their isolated effects are understood.

## First handoff prompt

> On resumption, read `AGENTS.md`, `RESEARCH.md`, `EXPERIMENTS.md`, `docs/measurement_protocol.md`, `docs/reproducibility.md`, `docs/roadmap-measurement.md`, `ROADMAP.md`, and the current sequence specification. Q09's portable M2-GQA-002 bundle verifies at SHA-256 `df79b3578c70c6fa7c3a0d0a2d34e02f257cec16c14295287d73912d2b9bef3a` with eight files; fixture reproductions validate plumbing but do not rerun the M2 comparison. Q10-PREP-002, Q11.a and Q11.b are complete; exact identities/hashes are in the Q10/Q11 result inventories, while source text and tokenizer artifacts remain ignored under `data/`. S02 state correctness passed without training; begin S03 dense local-attention correctness after checking the current tree. Q08 CUDA and sampled-CPU limitations and the independent M2 comparison rerun remain open. Keep the Q10 test model metrics sealed and sequence-module training gated on correctness plus a fresh matched three-seed control/candidate comparison.
