# Data preparation protocol

The current baseline accepts UTF-8 text files with one complete document per non-empty line. JSONL datasets must first be exported by selecting the text field into this format; the exporter should record its source dataset, revision, configuration, license, selection rule and export code version.

For a corpus with a sealed test set, run `frontier prepare-data --input SOURCE.txt --output data/corpus --validation-fraction 0.05 --test-fraction 0.05 --seed 17 --content-origin human` after verifying the source text is human-authored. Supply a JSON source sidecar with `--source-metadata` when available. Schema 4 writes `train.txt`, `validation.txt`, `test.txt` and a versioned `data_manifest.json`. It assigns normalized duplicates together, retains and reports duplicate rows, and records SHA-256 identities for each normalized document without placing document text in the manifest. `load_split` reads only training and validation documents; `load_all_splits` is the explicit access path for a final test evaluation. A zero `--test-fraction` keeps the legacy train/validation schema for existing workflows. Preparation refuses to overwrite a non-empty output directory; use a new directory for every corpus revision.

`content_origin` is one of `human`, `synthetic`, `mixed`, or `unknown`. The CLI defaults to source metadata when available and otherwise records `unknown`; older manifests also load as unknown. Do not infer origin from a dataset name, license, or the `synthetic_fixture` field. That legacy field identifies the built-in smoke fixture. TinyStories source metadata records `synthetic` explicitly. New source metadata should include dataset, revision, license and origin. The manifest hashes that metadata and its preprocessing contract; each run summary also hashes the exact manifest and reports UTF-8 byte counts and tokenizer token counts. The comparison tool permits narrow metric comparison with incomplete legacy records but blocks a research claim unless the tokenizer artifact, dataset source metadata, manifest and preprocessing identity are pinned.

The experiment configs refer to `data/tinystories` as a location only. No dataset is bundled or assumed licensed by this repository. Confirm the source license and usage terms, and use the same exported files for every control and candidate. Keep the data outside Git; record the manifest with each run. Tokenizers are saved as versioned JSON artifacts whose SHA-256 is recorded in both run and evaluation metadata. The byte tokenizer's artifact spells out the UTF-8 byte mapping and special-token contract; learned tokenizers must serialize their full vocabulary and merge/model data before they are eligible for comparison.

## Candidate corpus review

WikiText-103 raw was a plausible diagnostic candidate because its publisher describes full Wikipedia articles and the original language-modeling benchmark, but it was not selected. The [Salesforce dataset card](https://huggingface.co/datasets/Salesforce/wikitext) shows `cc-by-sa-3.0` and `gfdl` metadata tags while its licensing paragraph says CC BY-SA 4.0; the [publisher's loader at a pinned revision](https://huggingface.co/datasets/Salesforce/wikitext/blob/8aaa8b27d493dba10b8553290236799e6dc57829/wikitext.py) also declares CC BY-SA 4.0. The [Wikimedia Terms of Use](https://foundation.wikimedia.org/wiki/Terms_of_Use?useformat=mobile) describe page-level licensing and attribution, including imported text that may not be GFDL. The WikiText release does not provide the article revision and attribution mapping needed here to resolve which terms apply to every selected document. Both WikiText sizes are small, but the license and document-identity gaps remain. No WikiText files have been downloaded for this project.

## Q10.a corpus decision (23 September 2026)

Three primary-source candidates were reviewed before any corpus acquisition:

| Candidate | Source, scope and scale | License/provenance and local cost | Decision |
|---|---|---|---|
| WikiText-2 raw | The [Salesforce publisher card](https://huggingface.co/datasets/Salesforce/wikitext) describes English Wikipedia Good/Featured articles and reports 36,718 train, 3,760 validation and 4,358 test rows. It reports 4.72 MB of downloaded files and 18.26 MB total prepared footprint for the raw variant. | The card's metadata tags say `cc-by-sa-3.0` and `gfdl`; its licensing paragraph and pinned loader say CC BY-SA 4.0. Wikimedia terms require article-aware attribution and note that imported text may be CC BY-SA but not GFDL. The released rows do not preserve a source-page revision/attribution map, so the exact applicable terms are unresolved for this snapshot. | Rejected for this cycle despite the low footprint. Do not acquire unless the publisher supplies enough item-level provenance to resolve the license and document identity. |
| PG-19 | The [DeepMind release](https://github.com/google-deepmind/pg19/blob/master/README.md) describes English books published before 1919, split into 28,602 train books, 50 validation books and 100 test books, with about 1.97 billion training tokens. The [DeepMind account](https://deepmind.google/blog/a-new-model-and-dataset-for-long-range-memory/) presents it as a long-range book benchmark; the dataset author's [thesis](https://discovery.ucl.ac.uk/10126220/1/Jack%20Rae%20-%20Final%20Thesis.pdf) reports roughly 11 GB of text. | The release metadata names Apache-2.0 for the dataset compilation and identifies Project Gutenberg as the source for the underlying books. The full corpus exceeds the current bounded local-preparation target, and the publisher notes its dated style and historical biases. | Rejected for the initial control. It is a useful later long-context corpus, but its full footprint and dated literary domain are a poor fit for the first local baseline. |
| OpenStax *Principles of Economics 2e* | The [publisher's book page](https://openstax.org/books/principles-economics-2e/pages/preface) identifies a human-authored English economics textbook. The selected source is the publisher's [PDF asset](https://assets.openstax.org/oscms-prodcms/media/documents/Economics2e-OP_s2jF42u.pdf), whose embedded revision is `PE2-2017-002-(03/18)-LC`. The PDF response reported 51,763,377 bytes (about 49.4 MiB); this size has not been confirmed by a local download. | The book-specific publisher page states CC BY 4.0 and requires attribution to OpenStax and contributors. The official [CC BY 4.0 license](https://creativecommons.org/licenses/by/4.0/legalcode) applies. Preparation will extract text only, preserve source/title/revision attribution, exclude figures, and omit any passage separately marked under different terms. Expected raw storage is about 49.4 MiB plus extracted text; extracted size and preparation RAM remain unmeasured. | **Selected as a narrow educational-economics diagnostic**, not as a broad language-capability corpus. Q10.b must verify the downloaded asset hash and license markings before writing prepared text. If the text-only scope cannot be established, stop acquisition and retain synthetic-only claim scope. |

No corpus was downloaded or prepared for this decision. Q10.b owns the source-byte hash, deterministic text extraction, article identities, normalized duplicate handling, and train/validation/test split. The splitter now supports explicit held-out test output and hashed normalized document identities rather than repurposing validation. Fit the selected subword tokenizer on training documents only. Freeze validation and test article identities before tuning. Keep source and prepared text out of Git; commit only the source/license record, preparation code, hashes, and sanitized manifest.

### Q10.a correction — source-use restriction and replacement (23 September 2026)

The OpenStax choice above is withdrawn before acquisition. The current [book-specific publisher page](https://openstax.org/books/principles-economics-2e/pages/preface) states that the book may not be used to train large language models or otherwise ingested into generative-AI offerings without OpenStax's permission. The page also states CC BY 4.0, but that does not resolve the separate training restriction for this project. No OpenStax PDF or text was downloaded or used.

The replacement source is a bounded custom snapshot of English Wikipedia article revisions fetched by exact revision ID through the [official MediaWiki API](https://www.mediawiki.org/wiki/API%3ARevisions). This is not the WikiText release: each selected article must have a pinned revision permalink and item-level attribution. Wikimedia's [Terms of Use](https://foundation.wikimedia.org/wiki/Terms_of_Use?useformat=mobile) allow text reuse with attribution by linking to the source article, whose history identifies its contributors; the [developer guidance](https://foundation.wikimedia.org/wiki/Legal%3AWikimedia_Developer_App_Guidelines) says most Wikimedia text is CC BY-SA 4.0 and directs reusers to inspect the page footer, history, and discussion for item-specific terms. The official [CC BY-SA 4.0 legal code](https://creativecommons.org/licenses/by-sa/4.0/legalcode) requires attribution and ShareAlike for distributed adaptations. Q10.b must exclude non-text media, fair-use material, and pages with third-party or license notices that cannot be preserved, and store each exact oldid permalink with the prepared manifest. For API use, follow [API etiquette](https://www.mediawiki.org/wiki/API%3AEtiquette): identify the client, issue serial requests, and use `maxlag` for noninteractive runs. This correction supersedes the chapter-based split wording in the paragraph above: the split unit is a complete article revision.

This is a bounded English-encyclopedia diagnostic, not a broad capability benchmark. The source terms reviewed do not expressly discuss language-model training; treating reuse under the page's CC BY-SA terms as sufficient for this local research use is an interpretation, not an explicit AI-training grant. No source text or trained model will be published under this Q10 package. Any later distribution of derived artifacts needs a separate license review. Q10.b must use a descriptive API User-Agent, serial requests and `maxlag`, freeze the exact article/revision inventory before corpus acquisition, and stop if per-page licensing or attribution cannot be verified. The source API metadata endpoint was unavailable from the restricted checkout during the initial Q10.b preparation package; metadata access was later obtained through serial, metadata-only requests. No replacement corpus has been downloaded.

### Q10.b candidate inventory preregistration (23 September 2026)

Before querying revision metadata, freeze the candidate titles and eligibility rules in [`q10b_wikipedia_candidate_inventory.json`](q10b_wikipedia_candidate_inventory.json). The list contains 24 titles across six topical strata. Use every eligible title; do not select pages based on their content or model scores. At least 20 pages must pass. Each selected revision must be wikitext, at most 1 MiB, and the combined source revision size must not exceed 4 MiB. If the bound or minimum is not met, stop and amend the candidate-set decision before requesting any article bodies.

The metadata-only query requests title/page identity, latest revision ID, timestamp, byte size, SHA-1, content model, and edit comment. It does not request `rvprop=content`. The English Wikipedia [Revisions API](https://www.mediawiki.org/wiki/API%3ARevisions) documents these metadata fields separately from revision content. Use one request for all candidates where supported, otherwise serial batches, with `maxlag=5` and a descriptive User-Agent under [API etiquette](https://www.mediawiki.org/wiki/API%3AEtiquette). After revision IDs are frozen, inspect each exact revision's footer, history and discussion for the license and attribution conditions described in the [Wikimedia Terms of Use](https://foundation.wikimedia.org/wiki/Terms_of_Use?useformat=mobile) and [developer guidance](https://foundation.wikimedia.org/wiki/Legal%3AWikimedia_Developer_App_Guidelines). Do not fetch corpus text unless at least 20 pages pass every metadata, size, license and attribution rule. Record excluded pages and reasons; no silent substitutions.

The 24-title metadata query completed on 23 September 2026. All 24 revisions are namespace 0, non-redirect, main-slot wikitext; the largest is 331,340 bytes and the combined inventory is 3,272,433 bytes. The metadata inventory SHA-256 is recorded in [`q10b_wikipedia_candidate_inventory.json`](q10b_wikipedia_candidate_inventory.json). The first-pass page review found the default CC BY-SA 4.0 footer with “additional terms may apply,” plus copied/imported text, open-license attribution notices, and suspected model-generated-content notices on several pages. The audit covered no more than the newest 500 history entries at or before each pinned revision, not full page histories; therefore no candidate is cleared yet. See the inventory's `audit.page_findings` for the recorded holds. No raw revision-content API field was requested and no corpus text was saved or prepared. The auditors did load rendered oldid pages to read the requested footers; those responses included page HTML but the article text was not retained or used for corpus selection. Q10's body-acquisition gate remains closed until at least 20 pages have complete, traceable rights and attribution evidence.

For early infrastructure checks, `configs/smoke.json` creates a fixed synthetic fixture automatically. Its scores are not downstream language-capability evidence.

## Existing synthetic-domain control

The already-prepared M1 TinyStories subset can serve as a narrow architecture stability and cache-resource control while a natural-language corpus is reviewed. The pinned dataset card identifies the stories as synthetic and declares `CDLA-Sharing-1.0`. The [agreement](https://cdla.dev/sharing-1-0/) permits use of the data and places same-agreement conditions on publishing received or enhanced data; Section 3.5 says it imposes no restrictions on results. The [CDLA FAQ](https://cdla.dev/faq-resources/faq/) distinguishes those results from data that contains more than a de minimis portion of the source. Keep the raw and prepared text local; publish only aggregate results and provenance. This establishes a disclosed synthetic domain, not broad or human-authored language capability.

The experiment subset remains bytes 0-20,971,519 from the pinned training file and the existing deterministic 2% document holdout; its source revision and transformation are recorded below. Before reusing the local files, verify their manifest hashes against the recorded run artifacts. Do not silently rebuild or substitute the split.

The historical `data/tinystories/data_manifest.json` remains schema 1 and lacks current source-metadata, explicit-origin and preprocessing hashes; `load_split` therefore treats its origin as unknown. It remains unchanged. A separate local schema-3 split at `data/tinystories-v3` was prepared from the pinned 20 MiB prefix. The raw-prefix SHA-256 is `3232df12d6bdd8f83071f0d8196ad10128624168e228aaf9a23f6a1f8431ee8a`; the source-document SHA-256 is `5fe75b6ebc92b0cecc74b67e34aefc5d6552a71877c38bb09b12b2f63353995f`; train and validation hashes are `87ac33a8f47aeefdf772bd813472976eede4543d98d2c4122a1b07271163b44e` and `0ad8843aee6aff10a8bfc58fdfd80e26b261d061f341dffb12894a538f18a935`; the manifest SHA-256 is `5bac1fc6d9ab7af5ed4c9a61d3a5ccbd2426faebb75acf2b605b013e146841ac`. Its explicit origin is synthetic, not a test fixture. The split files are ignored local data and are not included in Git; run summaries must retain the manifest hash. This is eligible only for a scoped synthetic-domain comparison after the model configs and protocol are pinned.


## Reproducing the bounded TinyStories subset

The M1 corpus run uses bytes 0–20,971,519 of `TinyStories-train.txt` at the pinned `roneneldan/TinyStories` revision `f54c09fd23315a6f9c86f9dc80f725de7d8f9c64`. The [dataset card](https://huggingface.co/datasets/roneneldan/TinyStories/blob/f54c09fd23315a6f9c86f9dc80f725de7d8f9c64/README.md) declares `cdla-sharing-1.0` and describes the dataset as synthetic short stories. The experiment deterministically holds out 2% of selected documents from this training prefix; it does not use the dataset's separate validation file. This bounded split verifies training and evaluation plumbing, not broad capability.

`scripts/prepare_tinystories_prefix.py` splits on `<|endoftext|>`, discards the last partial story if the byte range cuts through one, and collapses story whitespace to single spaces. It writes one document per line and a provenance sidecar containing the dataset revision, source file, license, byte range, upstream size, raw-prefix SHA-256, complete-story count, discarded-tail size, and text transformation. It refuses to overwrite the downloaded prefix or either existing prepared output. Pass the sidecar to `frontier prepare-data --source-metadata`; the resulting split manifest is copied into every run. Raw and prepared corpus files remain local under the root `data/` directory, which is git-ignored.

### Q10.a amendment 2 — PMC Open Access metadata-only candidate screen (23 September 2026)

The Q10-SOURCE-001 Wikipedia screen remains in the experiment history: its 24 exact revisions met namespace/content-model/size rules, but zero pages had complete history and attribution evidence, so no body was requested. The completed Q10-SOURCE-002 screen used the [PMC Open Access Subset](https://pmc.ncbi.nlm.nih.gov/tools/openftlist/) and its official [article-dataset documentation](https://pmc-oa-opendata.s3.amazonaws.com/README.txt). PMC says article licenses vary by version and requires the item-level license to be followed; per-version JSON includes a license code, manuscript/OCR/retraction flags, XML URL and MD5. The source documentation also describes a daily S3 inventory, version behavior, approved retrieval services, required NLM acknowledgement, and possible third-party rights in media. See the [PMC Cloud documentation](https://pmc.ncbi.nlm.nih.gov/tools/pmcaws/), [search syntax](https://pmc.ncbi.nlm.nih.gov/about/userguide/), [OAI-PMH metadata service](https://pmc.ncbi.nlm.nih.gov/tools/oai/), and [copyright notice](https://pmc.ncbi.nlm.nih.gov/about/copyright/).

The executed six-stratum metadata screen followed the frozen selection in [`q10c_pmc_candidate_inventory.json`](q10c_pmc_candidate_inventory.json). First freeze 24 unique PMCID candidates by numeric query order, without inspecting or replacing metadata-ineligible candidates. Then inspect only PMC Cloud per-version JSON, S3 object-list metadata, and OAI-PMH compact Dublin Core (`metadataPrefix=oai_dc`); do not request full-text or `pmc_fm` records. Process only the OAI identifiers, title, creator, date, subject/citation and rights fields; discard any `dc:description` that contains abstract text. Because `license_code` identifies a Creative Commons family rather than its exact version, require that it maps to CC BY and that OAI `dc:rights` is exactly the [CC BY 4.0 legal URI](https://creativecommons.org/licenses/by/4.0/legalcode). Also require exactly one Cloud version per PMCID, non-manuscript/non-historical-OCR/non-retracted flags, a present XML object, and complete citation metadata. Retain the raw rights fields, canonical license URI, author/title/journal, publication year, DOI, exact PMCID version, source article URL, XML MD5, S3 modification date, and object size. Candidate XML is capped at 1 MiB per article and 4 MiB combined across all metadata-eligible candidates; at least 20 of the frozen 24 candidates must pass before any article XML may be requested through an approved PMC service. The six [MeSH topic queries](https://pmc.ncbi.nlm.nih.gov/about/userguide/) restrict this source screen to the MEDLINE-indexed subset; it does not support a general-English capability claim.

If a future source passes its metadata gate, inspect each exact XML license statement and any separately licensed notices. Exclude figures, captions and supplements unless their individual rights are verified, and exclude any text whose license or attribution cannot be traced. Require at least 20 documents to remain before preparation. Then use the existing schema-4 document-level split and normalized-duplicate grouping, fit the tokenizer on training documents only, and freeze the validation/test identities. At amendment 2 preregistration no PMCID candidates were selected; at amendment 3 closeout, 5 of 24 passed metadata review, the minimum-20 gate failed, and no article body or media was requested.

### Q10.a amendment 3 — PMC rights and publication-date screening clarification (23 September 2026)

The Q10-SOURCE-002 E-Utilities queries and all 24 frozen PMCID IDs are recorded in the candidate inventory. The completed metadata-only screen found 5 eligible versions (547,447 combined XML bytes) and 19 ineligible candidates, below the required 20. Failure categories overlap: 12 records are marked manuscripts, 10 have date-window/consistency failures, 5 have multiple available Cloud versions, and one carries CC BY 2.5 rights. PMC5738777 remains the first recorded failure. No article XML, body, figure or supplement was requested, so this candidate set is closed without acquisition.

For the frozen candidates, treat `dc:rights` as a multivalued field: at least one entry must exactly equal `https://creativecommons.org/licenses/by/4.0/`; record every entry, and fail a candidate if any other entry identifies a different CC license or a conflicting restriction. Copyright-holder text may be retained as a separate rights statement. Also require the Cloud citation year and any available OAI `dc:date` to resolve consistently within 2018–2025, matching the frozen search window. These clarifications did not change the 24 candidate IDs or permit replacement after metadata screening. The at-least-20 gate failed, despite the eligible XML size subtotal being below 4 MiB. No article XML may be requested under this amendment. A new amendment must precede any new source query.

### Q10.a amendment 4 — PMC publication-stage filtered candidate screen (23 September 2026)

Q10-SOURCE-002 is closed with 5 of 24 metadata-eligible versions (547,447 XML bytes); its at-least-20 gate failed. Twelve of the 24 records were flagged as author manuscripts, and five had multiple available Cloud versions. The official [PMC User Guide](https://pmc.ncbi.nlm.nih.gov/about/userguide/) documents the `"author manuscript"[filter]` and `preprint[filter]` query filters. The new screen applies both exclusions before candidate selection and creates a separate cohort; none of the earlier 24 IDs may be substituted into it.

The preregistered Q10-SOURCE-003 candidate rule specified six strata, eight unique IDs per stratum (48 total), ascending numeric PMCID order and the original stratum priority. Each query is capped at 1,000 IDs; do not replace ineligible candidates. Hash each returned ID list as UTF-8 of its exact numeric ID strings in original API order joined by commas, with no trailing comma. The selected-candidate digest uses UTF-8 `stratum_name<TAB>PMCID` rows in selection order, separated by LF without a trailing LF. The exact query, fields, eligibility rules, hashes and body gate are in [`q10d_pmc_published_candidate_inventory.json`](q10d_pmc_published_candidate_inventory.json). The intended screen retained the 2018–2025 date filter, CC BY search filter, exact canonical CC BY 4.0 OAI-rights rule, one-version requirement, 1 MiB per-article XML limit, 4 MiB aggregate limit and minimum 20 eligible versions. This cohort targets published biomedical/life-sciences articles and does not support a general-English capability claim.

This amendment was preregistered before the six E-Utilities ID queries, which were run once on 23 September 2026. All six ESearch translations turned the attempted `AND NOT` filters into positive AND terms; each warning list included `NOT` and `No items found.` All returned counts and ID lists were zero, so the 48-candidate selection gate failed before per-version metadata screening. The query strings, translations, warnings and empty-list hashes remain in [`q10d_pmc_published_candidate_inventory.json`](q10d_pmc_published_candidate_inventory.json). No article XML, body, figure, supplement or prepared corpus text was requested. Any corrected search is a separate attempt and must be preregistered before use.

### Q10.a amendment 5 — PMC Boolean grouping correction (23 September 2026)

The Q10-SOURCE-003 attempt failed before candidate selection because ESearch treated both excluded filters as positive AND terms when written as `AND NOT ... AND NOT ...`. The [PMC User Guide](https://pmc.ncbi.nlm.nih.gov/about/userguide/) documents uppercase Boolean operators and parenthesized grouping. The [NIH Preprint Pilot FAQ](https://pmc.ncbi.nlm.nih.gov/about/nihpreprints-faq/) shows a positive query followed by `NOT preprint[filter]` and states that E-utilities syntax requires a search term before Boolean `NOT`.

Q10-SOURCE-004 is a new, separately preregistered query attempt. It keeps the six topic strata, 48-candidate target, date/license filters, candidate order, per-version checks and size thresholds unchanged. Its query shape groups all positive terms first, then appends one exclusion clause: `(<positive terms>) NOT ("author manuscript"[filter] OR preprint[filter])`. The protocol adds a fail-closed ESearch translation check before proceeding to another stratum or selecting IDs. The exact expression, check, fields, hash encodings and body gate are in [`q10e_pmc_corrected_candidate_inventory.json`](q10e_pmc_corrected_candidate_inventory.json). None of the zero-ID Q10-SOURCE-003 results are reused.

At this amendment's preregistration, Q10-SOURCE-004's six queries had not been run. No new candidate IDs or per-version metadata had been requested, and no article XML, body, figures, supplements or prepared corpus text had been acquired. Article content remained gated on all 48 candidates being screened, at least 20 passing, and total eligible XML no more than 4 MiB.

#### Q10-SOURCE-004 closeout — metadata gate passed (23 September 2026)

All six preregistered ESearch translations preserved the terminal `NOT ("author manuscript"[Filter] OR "preprint"[Filter])` clause and emitted no standalone `NOT` warning. ESearch count/returned-list-length pairs were 961/961 for neuroscience, 8,368/1,000 for microbiology, 24,970/1,000 for plants, 2,080/1,000 for ecology, 58,571/1,000 for computational biology and 710,139/1,000 for public health. The capped lists produced 48 globally unique PMCID candidates, eight per stratum, selected by the frozen ascending-ID rule. Their selection SHA-256 is `8489310e65e4b0c65ffc1f59afdc93dd5ac841ddbe306294a5d8205c9a843adb`; complete ID lists, query translations and individual hashes are retained in [`q10e_pmc_corrected_candidate_inventory.json`](q10e_pmc_corrected_candidate_inventory.json).

Metadata-only screening passed 21 of 48 versions and rejected 27. The eligible strata were neuroscience (5), microbiology (8) and ecology (8); plants, computational biology and public health each had zero eligible records because every selected PMCID had multiple available PMC Cloud versions. Three neuroscience records failed the frozen date-consistency/window rule; PMC5738777 also had conflicting CC BY 2.5 rights. The 21 eligible XML objects total 3,153,456 bytes, with a maximum of 668,319 bytes each; no screening request failed. The inventory records exact XML object keys, MD5 values, S3 modification dates, sizes, Cloud metadata, OAI rights and dates. No article XML, `dc:description`, figure, supplement or prepared corpus text was requested.

The metadata gate therefore authorizes retrieval of only those 21 eligible XML objects through an approved PMC service. It does not clear the article text for training: inspect the exact article-level license statements and any separately licensed text or media, exclude unresolved material, and require at least 20 complete documents to remain before deterministic preparation. This cohort remains a narrow English, MEDLINE-indexed biomedical/life-sciences diagnostic; it does not support a general-English capability claim. The exact CC BY 4.0 check uses the canonical [Creative Commons legal code](https://creativecommons.org/licenses/by/4.0/legalcode) alongside PMC's item-level version metadata and [OAI-PMH record](https://pmc.ncbi.nlm.nih.gov/tools/oai/).

### Q10.a amendment 6 — article-level PMC rights audit (23 September 2026)

Before requesting article XML, rechecked the current primary service documentation. PMC reports that its Article Dataset distribution changes completed on 26 August 2026 and that legacy files were removed the week of 24 August; the current [PMC Cloud guide](https://pmc.ncbi.nlm.nih.gov/tools/pmcaws/) and [bucket README](https://pmc-oa-opendata.s3.amazonaws.com/README.txt) still document per-version XML objects and individual HTTPS/S3 retrieval. The current [OAI-PMH guide](https://pmc.ncbi.nlm.nih.gov/tools/oai/) identifies PMC Cloud, OAI-PMH, E-Utilities and BioC as the approved automated retrieval services, says license terms vary by article, and directs users to each article's license statement. Q10-SOURCE-005 uses only the current PMC Cloud HTTPS route; the retired OA Web Service is not used.

Q10-SOURCE-005 was preregistered at revision `dd6863cf1a6c7def5854c90d816dc0aa3c728bb` before any XML request. Its frozen 21-record allowlist and XML URLs are in [`q10f_pmc_article_rights_inventory.json`](q10f_pmc_article_rights_inventory.json); the allowlist digest is `4a2512aeafcc0d031464953041f0451ec8e1e1d65fec60354ff01755d2517a2a`. Retrieve only the exact version XML objects serially, verify response byte length plus XML MD5 against the frozen Cloud/S3 metadata, and record a SHA-256. Do not retrieve alternate versions, text files, PDFs, figures, supplements or media. Keep raw XML and all article text in ignored `data/`, never Git.

For each exact XML, confirm its PMCID/title/DOI identity and inspect the JATS article permissions/license element. Require the explicit canonical CC BY 4.0 URI and a consistent article statement; fail on missing, conflicting or unclear terms. Preserve the attribution fields, license URL, version, source URL, hashes and access date. Exclude references, acknowledgements, permissions, figures, tables, captions, supplements, media and non-prose markup from any later text extraction. Exclude a full article if separately licensed or untraceable rights apply to body prose and cannot be isolated. Include the NLM source acknowledgement required by the Cloud README and do not imply NLM endorsement. At least 20 complete articles must remain eligible before deterministic preparation; do not replace a failed candidate.

#### Q10-SOURCE-005 retrieval closeout and Q10-SOURCE-006 URI amendment (23 September 2026)

Q10-SOURCE-005 retrieved exactly the 21 frozen per-version XML objects serially from the current PMC Cloud HTTPS URLs. All 21 matched frozen byte lengths and MD5/ETags; the total was 3,153,456 bytes and per-file SHA-256 values are recorded in [`q10f_pmc_article_rights_inventory.json`](q10f_pmc_article_rights_inventory.json). There were 21 requests, no request or integrity errors, and no article prose extraction. XML remains only in ignored `data/q10-source-005/raw/`.

Before screening the 21-record cohort, a protocol pilot parsed the license element from PMC5798820 version 1 with DTD processing ignored and external resolution disabled. The JATS href was `http://creativecommons.org/licenses/by/4.0/`, while its license text began with the canonical HTTPS CC BY 4.0 URL and frozen OAI rights included the exact canonical HTTPS URI. No article prose was extracted, and no article rights decision was made. The original literal-HTTPS criterion in Q10-SOURCE-005 remains preserved.

Q10-SOURCE-006 prospectively permits only the two exact href strings `http://creativecommons.org/licenses/by/4.0/` and `https://creativecommons.org/licenses/by/4.0/`, with no port, query, fragment, credentials, or path variation. Preserve the raw href; normalize only the HTTP form to canonical HTTPS for URI identity comparison. The article text must still identify CC BY 4.0, the frozen OAI rights must contain the exact canonical HTTPS URI, and no conflicting license, restriction, or unresolved third-party prose may apply. Missing or ambiguous evidence fails. The [official Creative Commons deed](https://creativecommons.org/licenses/by/4.0/) resolves the HTTP reference to the deed and identifies HTTPS as the canonical URL; the [legal code](https://creativecommons.org/licenses/by/4.0/legalcode) confirms the canonical URL. This supports a narrow protocol interpretation about URI identity, not a legal determination about any article's contents.

Q10-SOURCE-006 was frozen before cohort rights decisions. Its application checked the PMCID, version, title and DOI against the frozen metadata and required an exact allowed XLink `href` within `front/article-meta/permissions/license` or its descendants, plus consistent license text and canonical HTTPS in the frozen OAI rights. All 21 identities, OAI values and license-text signals matched. Thirteen records contained a permitted href. Eight—PMC11261691, PMC11261710, PMC11281480, PMC11281800, PMC11288634, PMC11316580, PMC11349053 and PMC11350378—contained a canonical NISO ALI `license_ref` and matching CC BY 4.0 statement but no href attribute. Because at most 13 could pass the later body-rights screen, below the frozen 20-document minimum, stop the cohort before screening body-level third-party notices. No document is fully rights-cleared; no article prose, prepared corpus or tokenizer was produced, and no training occurred. This is a failure of the predeclared evidence gate, not a conclusion that the eight articles are unlicensed. Do not substitute candidates or retroactively count a `license_ref` element as an href. Select and preregister a new source/cohort before further content acquisition.

#### Follow-up standards review after Q10-SOURCE-006

After the explicit-href gate failed, the primary [NISO ALI `license_ref` element definition](https://jats.niso.org/1.1/Blue1_1/Blue1_1-elem-ali_license_ref.html) was checked: it defines the element as a reference to a URI carrying the license terms. The [JATS `<license>` content model](https://jats.nlm.nih.gov/tools/element-list/nlm_jats/el-license.html) includes `ali:license_ref` among permitted children. The JATS license documentation also describes it as performing the license-pointer function and recommends omitting `xlink:href` when ALI is used; this is consistent with the eight observed XML records. This standards finding does not change the Q10-SOURCE-005/006 outcome or the eight records' recorded status under that frozen criterion.

Q10-SOURCE-007 was preregistered at clean revision `e3506d99ac653e4315f8ed0130f9458ad395c832` as a separate method re-audit of the same 21 frozen XML objects. It accepts the exact canonical HTTPS CC BY 4.0 URI as the text content of a correctly namespaced NISO `ali:license_ref`, alongside the two exact XLink href forms already admitted by Q10-SOURCE-006. Its exact input and rule are in [`q10g_pmc_ali_rights_audit.json`](q10g_pmc_ali_rights_audit.json). It preserves the older outcome, makes no additional requests, adds no candidates, and requires the same OAI/text consistency, identity, conflicting-rights and full body-exception checks. At preregistration no SOURCE-007 body-rights review had occurred. Do not proceed to corpus extraction unless at least 20 complete documents pass the full audit.
