# Experiment log

This log records planned and completed experiments. Entries are never backfilled with expected results. Each completed result must link to its run artifact; preserve failures and incompatible comparisons. A change in budget or procedure creates a new run/experiment revision.

## M1-001 — Offline fixture smoke check

- **Status:** Completed; CPU FP32, PyTorch 2.14.0, seed 17.
- **Hypothesis:** The byte tokenizer, causal model, optimizer and loss path can overfit a tiny fixed corpus.
- **Change:** Train the smoke configuration on the checked-in deterministic fixture.
- **Control:** Same 2-layer conventional GQA Transformer at initialization; this fixture check is not a capability comparison.
- **Training budget:** 30 optimizer updates, 7,680 byte tokens, approximately 1.427 billion estimated dense-decoder FLOPs (estimator v1).
- **Evaluation metrics:** Final train loss 4.678; held-out NLL 4.824 and perplexity 124.504 over 2,180 fixture tokens; 22,784 parameters; FP32 tensor bytes 91,168; train throughput 41,174 tokens/sec measured over optimizer-active time. CPU RSS/VRAM and inference profiles are recorded in the artifact.
- **Result:** Training, evaluation, checkpoint load, and profiling completed. Full-sequence versus chunked-cache logits differed by at most `1.79e-7` over 8 tokens. Machine-readable artifacts are in [`runs/smoke-verified/summary.json`](runs/smoke-verified/summary.json), [`runs/smoke-verified/profile.json`](runs/smoke-verified/profile.json) and [`runs/smoke-verified/metrics.jsonl`](runs/smoke-verified/metrics.jsonl).
- **Interpretation:** The small model learns the synthetic fixture and validates the end-to-end path. These synthetic held-out results do not measure language capability or support an architecture claim.
- **Next experiment:** Resume equivalence, then train the prepared-corpus baselines A and B.

## M1-002 — Resume determinism

- **Status:** Completed; deterministic CPU FP32 test, PyTorch 2.14.0.
- **Hypothesis:** Checkpointing optimizer, scheduler, RNG and data-sampler state reproduces an uninterrupted short run in the same environment.
- **Change:** Compare 4 uninterrupted updates to 2 updates plus checkpoint/reload plus 2 updates.
- **Control:** Uninterrupted run, identical config and seed.
- **Training budget:** 4 optimizer updates per path; 128 byte tokens.
- **Evaluation metrics:** Model/optimizer tensor equality where deterministic kernels permit, final NLL, sampled batch/index stream, total tokens and estimated FLOPs.
- **Result:** Model tensors were bitwise equal after both paths in `tests/test_resume.py`. The full suite passes: 24 tests, including [tests/test_resume.py](tests/test_resume.py) and worktree-provenance coverage.
- **Interpretation:** Same-environment exact resume works for this deterministic CPU test. It does not establish equivalence across devices or software versions.
- **Next experiment:** Keep the same checks in the baseline integration test and begin corpus runs.

## M1-003 — Conventional baseline A

- **Status:** Completed for seed 17; Windows 11 CPU FP32, PyTorch 2.14.0.
- **Hypothesis:** The configured conventional decoder-only Transformer can learn from a prepared language corpus at small scale.
- **Change:** Train the 4-layer, width-128 decoder with UTF-8 byte tokenization, context 256, pre-norm RMSNorm, RoPE, GELU FFN, and tied embeddings.
- **Control:** Same architecture at initialization; this is a learning check, not a method comparison.
- **Training budget:** 2,097,152 input tokens, 1,024 optimizer updates, 13,625,783,746,560 estimated dense-decoder FLOPs (estimator v1), seed 17.
- **Evaluation metrics:** Validation NLL 1.50373 and perplexity 4.49843 over 411,608 scored tokens; 33,046 training tokens/sec; 820,736 parameters; 3,283,200 FP32 tensor bytes; 3,297,333-byte weights artifact; peak training RSS 663,212,032 bytes.
- **Result:** The run completed on the pinned TinyStories 20 MiB training-file prefix with the deterministic 2% document holdout. Artifacts: [summary.json](runs/tinystories-a-seed17/summary.json), [summary.csv](runs/tinystories-a-seed17/summary.csv), [profile.json](runs/tinystories-a-seed17/profile.json), and [data_manifest.json](runs/tinystories-a-seed17/data_manifest.json).
- **Interpretation:** End-to-end training and held-out perplexity evaluation work on a real prepared corpus. TinyStories is a synthetic short-story dataset and this byte-token setup does not measure broad language capability.
- **Next experiment:** Repeat the same recipe and token budget across the predeclared seeds in M1-004.

## M1-004 — Baseline A seed repeats

- **Status:** Completed; seeds 17, 42, and 123 on the same CPU FP32 environment.
- **Hypothesis:** Baseline A's held-out metric is not an artifact of one initialization seed.
- **Change:** Repeat baseline A without changing the data split, token budget, optimizer, tokenizer, or evaluation protocol.
- **Control:** The other baseline A seeds, paired by seed with baseline B in M1-005.
- **Training budget:** 2,097,152 input tokens and 13,625,783,746,560 estimated FLOPs per seed; 6,291,456 input tokens total.
- **Evaluation metrics:** Per-seed perplexities were 4.49843, 4.66806, and 4.54206. Mean perplexity was 4.56952 with sample standard deviation 0.08809; mean NLL was 1.51928 ± 0.01920. Training throughput was 33,046–34,355 tokens/sec.
- **Result:** All three runs completed. The aggregate and paired comparison are in [tinystories-ab-seeds.json](runs/tinystories-ab-seeds.json); individual summaries are [seed 17](runs/tinystories-a-seed17/summary.json), [seed 42](runs/tinystories-a-seed42/summary.json), and [seed 123](runs/tinystories-a-seed123/summary.json).
- **Interpretation:** The three-seed baseline is stable on this particular held-out split. All run environments report a null Git revision because the initial repository had not yet been committed; the comparison harness therefore blocks an improvement claim.
- **Next experiment:** Train baseline B with matched estimated compute and the same three seeds.

## M1-005 — Conventional baseline B, compute matched

- **Status:** Completed; seeds 17, 42, and 123 on the same CPU FP32 environment as baseline A.
- **Hypothesis:** A larger conventional model might improve held-out language modeling under baseline A's estimated training-FLOP budget.
- **Change:** Train the 6-layer, width-256 decoder with the same tokenizer, context length, optimizer family, data split, and evaluation protocol.
- **Control:** Baseline A paired by seed.
- **Training budget:** 407,552 input tokens and 13,631,773,212,672 estimated FLOPs per seed. This is a 0.04396% higher estimated budget than baseline A; the token-count difference is disclosed.
- **Evaluation metrics:** Baseline B mean perplexity was 7.77399 ± 0.12621 and mean NLL was 2.05070 ± 0.01627 across seeds. The paired perplexity delta, candidate minus baseline A, was +3.20447 ± 0.05706. Each pair passed the data, tokenizer, evaluation, recipe, seed, environment, and 1%-compute checks. The 3-seed metric comparison is marked comparable; improvement-claim eligibility is false because every run lacks a pinned Git revision.
- **Result:** Baseline B perplexity was higher on all three paired seeds. B has 4,788,224 parameters and 19,153,280 FP32 tensor bytes versus A's 820,736 parameters and 3,283,200 bytes. At batch 1, prompt 32 plus 32 cached decode tokens, A/B measured 15,755/9,002 prefill tokens/sec, 617/385 decode tokens/sec, and 258,048/774,144 KV-cache bytes. Peak training RSS was 663,212,032 bytes for A and 882,659,328 for B. Full paired results are in [tinystories-ab-seeds.json](runs/tinystories-ab-seeds.json); profiles are [baseline A](runs/tinystories-a-seed17/profile.json) and [baseline B](runs/tinystories-b-seed17/profile.json); all six per-run CSV summaries are alongside their JSON summaries.
- **Interpretation:** Under this narrow TinyStories, byte-token, and fixed-compute setup, the larger model used more parameters and memory, ran slower on CPU, and had worse held-out perplexity. This is a metric result for the disclosed setup, not a general architecture claim; no improvement is claimed. No CUDA or energy sensor was available.
- **Next experiment:** Pin the source revision before using this comparison as an improvement claim; then begin Milestone 2 with interchangeable sequence blocks and the same corpus, seed, and compute controls.

## M1-006 — Inference cache and profiling checks

- **Status:** Completed on offline fixture and TinyStories baseline checkpoints; Windows CPU FP32, PyTorch 2.14.0.
- **Hypothesis:** Incremental cached decoding agrees numerically with full-sequence evaluation, and the memory/throughput profiler uses consistent workloads.
- **Change:** Profile checkpoints with fixed prompt lengths and generation lengths.
- **Control:** Full-sequence logits and analytically calculated KV allocation for the same model.
- **Training budget:** None; report checkpoint provenance and inference device.
- **Evaluation metrics:** Logit difference, cache bytes, model bytes, process/device peak memory, prefill latency and decode latency/tokens per second.
- **Result:** On the smoke model, actual and analytic cache bytes matched at 3,840, 7,936 and 14,080 bytes; cached/full-sequence max logit error was 1.79e-7. On TinyStories A/B with a 32-token prompt and 32-token cached decode, cache bytes were 258,048/774,144, prefill throughput was 15,755/9,002 tokens/sec, and decode throughput was 617/385 tokens/sec. All profile and comparison outputs are linked from M1-003 and M1-005. Peak CPU process RSS during A/B inference profiling was 226,521,088/292,102,144 bytes; no CUDA device or energy sensor was available.
- **Interpretation:** Cache semantics and byte accounting agree for standard attention. The CPU timings are local measurements, not hardware-general performance claims.
- **Next experiment:** Profile interchangeable sequence blocks under the same prompts, token budget and evaluation protocol in Milestone 2.

## M1-007 — Matched-compute smoke baseline pair

- **Status:** Completed; CPU FP32, PyTorch 2.14.0, seed 17.
- **Hypothesis:** The comparison harness can verify a training-compute-matched control pair and refuse a scientific capability claim from synthetic data.
- **Change:** Train a 1-layer, width-16 baseline A and a 2-layer, width-16 baseline B with the same byte tokenizer, fixture, seed, context, batch, optimizer and evaluation protocol.
- **Control:** Baseline A, 120 updates and 1,920 input tokens.
- **Training budget:** Baseline B used 88 updates and 1,408 tokens. Estimator v1 measured 74,833,920 versus 74,612,736 FLOPs, a 0.296% difference.
- **Evaluation metrics:** Fixture NLL/perplexity, run-comparison checks, parameters, tensor bytes, peak process memory, KV bytes and CPU prefill/decode throughput.
- **Result:** A scored NLL 4.2921/perplexity 73.1221 with 5,984 parameters and 23,944 tensor bytes; B scored NLL 4.6785/perplexity 107.6103 with 7,808 parameters and 31,248 tensor bytes. The comparator verified matching data, tokenizer, evaluation protocol, training recipe, seed and environment; comparable_for_metric_comparison is true and comparable_for_improvement_claim is false because evidence scope is synthetic infrastructure fixture. For the 8-token prompt profile, A measured 12,674 prefill tokens/sec and 2,352 decode tokens/sec with 704 KV bytes; B measured 7,779 prefill tokens/sec and 1,325 decode tokens/sec with 1,408 KV bytes. These tiny CPU timing results are functional checks, not performance evidence. Artifacts: [runs/m1-smoke-a/summary.json](runs/m1-smoke-a/summary.json), [runs/m1-smoke-a/profile.json](runs/m1-smoke-a/profile.json), [runs/m1-smoke-b/summary.json](runs/m1-smoke-b/summary.json), [runs/m1-smoke-b/profile.json](runs/m1-smoke-b/profile.json), and [runs/m1-smoke-comparison.json](runs/m1-smoke-comparison.json).
- **Interpretation:** The matched-compute comparison and synthetic-evidence guard work. The two fixture scores do not establish language capability or an architecture improvement.
- **Next experiment:** Prepare a licensed corpus and run the published baseline A/B configs with the predeclared equal-compute protocol.

## M1-008 — Pinned multi-seed reproducibility and comparison

- **Status:** Completed; Windows 11 CPU FP32, PyTorch 2.14.0, source revision `7e518c4a24171444a5c0c613cc126346d19bb178`.
- **Hypothesis:** Once every run records a pinned source revision and distinct matched seeds, the comparison harness can verify the full experiment provenance and assess the larger conventional model under an equivalent estimated training-compute budget.
- **Change:** Re-run Baseline A and B with seeds 17, 42 and 123 against the same bounded TinyStories document split. Seed 17 reuses the correctly configured pinned runs; seeds 42 and 123 use seed-specific configs. Architecture, optimizer recipe, corpus, tokenizer and evaluation protocol remain fixed within each model group.
- **Control:** Baseline A, paired with Baseline B by seed. Both groups use the same source revision, train/validation hashes, UTF-8 byte tokenizer, CPU/software environment and evaluation protocol.
- **Training budget:** Per seed, A used 2,097,152 input tokens and 13,625,783,746,560 estimated FLOPs; B used 407,552 input tokens and 13,631,773,212,672 estimated FLOPs, 0.04396% more. Each group contains three seeds. The estimator is `decoder-dense-v1`; the FLOPs are estimates, not hardware counters.
- **Evaluation metrics:** A validation perplexity was 4.49843, 4.66806 and 4.54206 (mean 4.56952, sample SD 0.08809); B was 7.63929, 7.88951 and 7.79318 (mean 7.77399, sample SD 0.12621). The paired B-minus-A perplexity delta was +3.20447 (sample SD 0.05706); paired mean-NLL delta was +0.53141 (sample SD 0.00770). Training throughput ranged from 29,118–30,737 tokens/sec for A and 7,869–7,909 for B. At batch 1, 32 prompt tokens and 32 cached decode tokens, A/B measured 13,287/6,917 prefill tokens/sec, 591/361 decode tokens/sec, 258,048/774,144 actual KV-cache bytes, and 226,611,200/292,012,032 peak process RSS bytes. Parameter counts were 820,736/4,788,224; FP32 tensor sizes were 3,283,200/19,153,280 bytes. No CUDA device or energy sensor was available.
- **Result:** The three-seed comparator passed all corpus, tokenizer, evaluation, environment, clean-worktree, architecture, recipe, seed and 1%-compute checks. It records `comparable_for_metric_comparison: true`, `source_worktrees_clean: true`, and `comparable_for_improvement_claim: true`; this field means the evidence passes the reporting gates and does not mean the candidate improved. Baseline B had higher perplexity on every paired seed. The full comparison is [`runs/verified-tinystories-ab-seeds.json`](runs/verified-tinystories-ab-seeds.json). Run summaries: [A seed 17](runs/pinned-a-seed17/summary.json), [A seed 42](runs/verified-a-seed42/summary.json), [A seed 123](runs/verified-a-seed123/summary.json), [B seed 17](runs/pinned-b-seed17/summary.json), [B seed 42](runs/verified-b-seed42/summary.json), and [B seed 123](runs/verified-b-seed123/summary.json). Each run directory also contains its `profile.json`, `summary.csv`, environment, data manifest and checkpoint artifacts.
- **Interpretation:** Under this small synthetic-story corpus, byte-tokenizer and fixed-compute setup, the larger model used substantially more resident and KV memory, trained and decoded more slowly on CPU, and had worse held-out perplexity. This is a trustworthy result for the disclosed narrow setup; it does not establish general language capability or support a novel-architecture conclusion. The pinned comparator is now able to report the result, and the result is not an improvement.
- **Next experiment:** Begin Milestone 2 with one interchangeable sequence-block change at a time, using Baseline A as the control, the same pinned corpus/evaluation protocol, at least three matched seeds and an equivalent estimated-compute budget.
- **Protocol note:** The first pinned rerun attempt named folders for seeds 17/42/123 while leaving the configs at seed 17. `compare-seeds` rejected the duplicate seeds. Those duplicate-seed folders (`runs/pinned-a-seed42`, `runs/pinned-a-seed123`, `runs/pinned-b-seed42`, and `runs/pinned-b-seed123`) are excluded from this result and retained as audit artifacts. The corrected runs above use their actual configured seeds. The clean-worktree field was added after these runs; it was audited from the clean status recorded before the training set and the absence of tracked source changes through training. Their environment files mark this field as retroactively verified. Future runs capture it automatically; the CLI also supports a `--seed` override.

## Reporting erratum — corpus origin and legacy eligibility

The M1 TinyStories runs use synthetic short stories. In the version of the run schema used for M1, `synthetic_fixture: false` meant only that data came from a prepared corpus instead of the built-in smoke fixture. It did not mean the text was human-authored. The old comparator had no separate `content_origin` field and could mark the M1-008 report `comparable_for_improvement_claim: true` without checking this distinction.

The recorded perplexities, compute estimates, and negative direction remain the results of those runs. Interpret them only as a narrow TinyStories corpus comparison; do not cite the legacy eligibility flag as evidence of broad capability or natural-language generalization. New comparisons require an explicit origin value. Missing origin is recorded as unknown and blocks improvement eligibility. The original run summaries are preserved unchanged.

The linked `runs/` files are local ignored artifacts and are not present in a public clone. Until the portable evidence export is implemented, use the numerical results in this log as the public record and treat artifact links as author-local references.

## M2-GQA-000 - GQA training and inference plumbing smoke

- **Status:** Completed 23 September 2026 on clean revision `bad3aa4`, Windows CPU FP32, PyTorch 2.14.0.
- **Hypothesis:** The existing GQA implementation can train, checkpoint, resume, evaluate and profile against the new schema-3 split, with cached logits matching full-sequence logits and KV storage scaling with the number of KV heads.
- **Change:** Run the seed-17 MHA and GQA smoke configs for 32 updates each. Stop after update 16, resume from the saved checkpoint, then evaluate and profile both completed checkpoints.
- **Control:** MHA with four KV heads. GQA changes only KV heads to two; both use the same data, tokenizer, seed, model width/depth, optimizer, context and batch.
- **Training budget:** 65,536 tokens and 32 updates per arm. Estimated FLOPs were 425,352,757,248 for MHA and 399,582,953,472 for GQA (-6.058%); this smoke is intentionally not compute-equivalent and cannot support an efficiency comparison. Each arm resumed after 16 updates and completed without a recorded loss or gradient failure.
- **Evaluation metrics:** Full validation NLL, perplexity and bits per UTF-8 byte; parameter count and tensor bytes; training throughput and peak process RSS; actual KV-cache bytes; prefill and cached-decode throughput; cached/full logit error. The held-out split scored 411,608 next-token targets and 411,142 UTF-8 bytes per arm.
- **Result:** MHA/GQA mean NLL was 4.13559/4.06565, perplexity 62.5262/58.3025, and bits per byte 5.96386/5.86259. Parameter counts were 820,736/755,200; FP32 tensor bytes were 3,283,200/3,021,056. At prompt lengths 32/128/224 followed by 32 decode tokens, cache bytes were 262,144/655,360/1,048,576 for MHA and exactly half for GQA. At prompt 32, median prefill throughput was 14,710/15,407 tokens/sec and cached decode was 658.8/644.8 tokens/sec. Training throughput was 29,543/29,184 tokens/sec; peak process RSS was 645,406,720/650,993,664 bytes. Cached/full logit error was 6.85e-7/4.77e-7. Artifacts: [MHA summary](runs/m2-gqa-001-smoke/mha-seed17/summary.json), [MHA profile](runs/m2-gqa-001-smoke/mha-seed17/profile.json), [GQA summary](runs/m2-gqa-001-smoke/gqa-seed17/summary.json), and [GQA profile](runs/m2-gqa-001-smoke/gqa-seed17/profile.json).
- **Interpretation:** Resume, evaluation, profiling and cache parity work. GQA halves the measured cache for these workloads. The NLL values are from a deliberately tiny training budget; GQA's CPU decode was slightly slower at all three lengths. This is a plumbing result, not a capability or performance claim. No CUDA device or energy sensor was available.
- **Next experiment:** The 5%-compute seed-17 screen is recorded in M2-GQA-001. Use its result only to decide whether the fresh three-seed matched-compute confirmation is worth running.

## M2-GQA-001 - Grouped-KV attention screen and confirmation

- **Status:** The seed-17 5%-compute screen is complete on clean revision `bad3aa4`. The first full-budget attempt completed all six runs on clean revision `d12da08`, but the comparator rejected every pair because the warmup fractions differed by 6.055%, beyond its 5% recipe gate. It is retained as protocol-ineligible descriptive evidence; no paired claim is made. Corrected confirmation M2-GQA-002 is preregistered below. The schema-3 split is local at `data/tinystories-v3`; its raw prefix, synthetic origin, source metadata, split hashes and manifest hash are recorded in `docs/data_protocol.md`. The M1 schema-1 data remains unchanged.
- **Hypothesis:** Reducing KV heads from four to two will halve standard-attention cache storage at the same layer, batch, context and dtype, while preserving enough short-story language modeling quality to justify the configuration.
- **Change:** Create immutable `configs/m2/tinystories_v3_mha.json` and `configs/m2/tinystories_v3_gqa.json` copies of `configs/baseline_a.json` and `configs/gqa_a.json`, pointing both at `data/tinystories-v3`. The candidate changes KV heads from four to two. Match the `decoder-module-mac-v1` estimated FLOP budget by using 2,097,152 MHA tokens (1,024 optimizer updates) and 2,232,320 GQA tokens (1,090 updates); the planned totals are 13,611,288,231,936 and 13,610,794,352,640 estimated FLOPs, respectively (-0.00363%). Keep the byte tokenizer, model width/depth, FFN, normalization, positional encoding, optimizer and evaluation split fixed.
- **Control:** A fresh MHA-A run from the immutable `configs/m2/tinystories_v3_mha.json` copy, same seed, source revision, data split and evaluation protocol. The existing M1 runs use the older estimator and are not the control for this experiment.
- **Training budget:** The seed-17 screen used five warmup updates and approximately 5% of the full per-seed budget: 51 MHA updates / 104,448 tokens / 677,905,956,864 FLOPs versus 54 GQA updates / 110,592 tokens / 674,296,233,984 FLOPs, a -0.53248% candidate delta. The completed but ineligible confirmation attempt used three paired seeds; per seed MHA used 2,097,152 tokens / 1,024 optimizer updates / 13,611,288,231,936 estimated FLOPs and GQA used 2,232,320 tokens / 1,090 updates / 13,610,794,352,640 estimated FLOPs, a -0.00363% delta. The corrected confirmation repeats these budgets.
- **Evaluation metrics:** Primary full-split validation mean NLL; report the paired candidate-minus-MHA mean and every seed delta. The predeclared practical loss budget is +0.02 nats/token, approximately the M1-A between-seed SD (0.01920) and about a 2% perplexity increase on this synthetic diagnostic. This is a decision threshold, not a formal significance test or broad capability claim. Also report perplexity, byte-normalized bits per UTF-8 byte, model tensor bytes, estimated training FLOPs, fixed-workload KV-cache bytes, prefill tokens/sec, cached-decode tokens/sec, and process peak RSS. The pilot is a stability/resource check only.
- **Result:** At seed 17, MHA used 104,448 tokens, 51 updates and 677,905,956,864 estimated FLOPs; GQA used 110,592 tokens, 54 updates and 674,296,233,984 FLOPs (-0.53248%). Validation NLL was 3.65896 for MHA and 3.55617 for GQA (candidate-minus-control -0.10280); perplexity was 38.8210 and 35.0286. Both runs completed with finite training state and the same recorded data manifest and source revision. Artifacts: [MHA screen summary](runs/m2-gqa-001-screen/m2-gqa-001-screen-mha-seed-17-53d27a081c/summary.json) and [GQA screen summary](runs/m2-gqa-001-screen/m2-gqa-001-screen-gqa-seed-17-384ff2890d/summary.json).
- **Full-budget protocol audit:** All six attempted runs completed their planned budgets on clean revision `d12da083126dfbc16c1bcbd4f8854ddc48bf7b94`. Each seed pair used the same data, tokenizer, evaluation, environment and source revision; estimated training FLOPs differed by -0.003628%. However, both configs used 100 warmup updates across 1,024 versus 1,090 total updates, giving a 6.055% relative warmup-fraction difference. `frontier compare-seeds` correctly set `comparable_for_metric_comparison: false` and `comparable_for_improvement_claim: false` for all pairs because `same_training_recipe` failed. Descriptive paired NLL deltas were -0.001536, -0.051819 and -0.013434 nats/token (mean -0.022263, sample SD 0.026278); these values are not an eligible estimate of the architecture effect. Mean NLL was 1.519285 for MHA and 1.497022 for GQA. At prompt length 32 plus 32 cached decode tokens, cache bytes were exactly 262,144 versus 131,072; parameter counts were 820,736 versus 755,200 and FP32 tensor bytes were 3,283,200 versus 3,021,056. Mean CPU prefill was 15,567 versus 14,110 tokens/sec and cached decode was 632.2 versus 635.7 tokens/sec; these small-run timings are descriptive only. No CUDA or energy sensor was available. Comparator artifact: [local ineligible report](runs/m2-gqa-001-comparison.json); run summaries and profiles remain local under `runs/m2-gqa-001-confirm/`.
- **Interpretation:** The one-seed screen justified attempting confirmation, but it is not evidence of a capability improvement. The first full-budget attempt failed the preregistered recipe-equivalence gate. The smoke separately confirmed half-sized KV storage; the screen did not profile latency.
- **Next experiment:** Run M2-GQA-002 below from a new clean revision. It matches normalized warmup schedule fractions within the comparator's frozen 5% tolerance and repeats both arms for all three seeds.


## M2-GQA-002 - Corrected grouped-KV confirmation (preregistered)

- **Status:** Completed 23 September 2026 on clean revision `73bde6e9dfa139f75a77fe7dae3bf710c3514678`. All six planned jobs finished at their exact token budgets. `compare-seeds` reports `comparable_for_metric_comparison: true`, `comparable_for_improvement_claim: true`, and synthetic-corpus scope; the human-corpus claim remains false. Plan digest `4f7ab8d7fe88d68617df089d720f2fcded60e0a823b5f4fc62e5928c50ebedf1`; comparison artifact SHA-256 `28fec3e5f0d583784e73cd0f792d14d9cb86790b9a0487628027d4914a1f4d25` (local ignored artifact).
- **Hypothesis:** Reducing KV heads from four to two halves standard-attention cache storage while preserving short-story language-model loss within the previously declared +0.02 nats/token practical decision budget.
- **Change:** Use versioned configs `configs/m2/tinystories_v3_mha_gqa002.json` and `tinystories_v3_gqa_gqa002.json`. Retain MHA warmup at 100 updates; set GQA warmup to 106 to match the fraction of the full training schedule. Fractions are 9.765625% and 9.724771%, a 0.418349% relative difference, within the existing 5% `same_training_recipe` gate. No comparator threshold or code is changed. The only model change is KV heads; the only optimizer-schedule adjustment is GQA warmup scaled to its 1,090-update run length. Data, tokenizer, all other model and optimizer fields, evaluation and profiling remain fixed. Config SHA-256: MHA `3bfbf0c64c8443afc105e3e327f3c430d8e7274c53faee372ecdd105e001d5dd`, GQA `43fb5ed8fc10c63f7dcec1e92cda61df02419c5d2360de59110c417a0293081c`, sweep spec `6023b478e7f62174ea84ef08f181af3c7faeda4e5b3c5605494f18dc3a92d88b`.
- **Control:** Fresh MHA with four query heads and four KV heads at seeds 17, 42 and 123. Pair each run with a fresh GQA candidate at the same seed. All six runs use one clean committed revision, the schema-3 TinyStories split and the pinned byte tokenizer.
- **Training budget:** Per seed, MHA uses 2,097,152 tokens / 1,024 updates / 13,611,288,231,936 estimated FLOPs; GQA uses 2,232,320 tokens / 1,090 updates / 13,610,794,352,640 estimated FLOPs (-0.003628%). Total repeat cap: 85,000,000,000,000 FLOPs; planned repeat total: 81,666,247,753,728. Including the prior ineligible full attempt, cumulative Q07 confirmation-attempt compute is 163,332,495,507,456 estimated FLOPs. FLOPs are estimator outputs, not hardware counters.
- **Evaluation metrics:** Full validation mean NLL as primary; per-seed and mean paired GQA-minus-MHA deltas with sample SD; perplexity and UTF-8 bits/byte; actual tensor and parameter bytes; cache/state bytes at prompts 32/128/224 with 32 decode tokens; serial CPU prefill/decode timings, training throughput, process peak RSS and cached/full-logit parity. Report CUDA and energy as unavailable with reason. The +0.02 nats/token limit is a practical decision threshold, not a significance test; evidence remains limited to synthetic TinyStories.
- **Result:** The three paired GQA-minus-MHA NLL deltas were -0.002924, -0.050193 and -0.016104 nats/token (mean -0.023074, sample SD 0.024393); all three seeds favored GQA on this validation metric. Mean NLL was 1.519285 (SD 0.019201) for MHA and 1.496211 (SD 0.005209) for GQA; mean perplexity was 4.569519 (SD 0.088087) and 4.464781 (SD 0.023239); mean bits/UTF-8-byte was 2.175834 (SD 0.027368) and 2.142239 (SD 0.008041). Each run scored 411,608 next-token targets and 411,142 UTF-8 bytes. The paired mean clears the preregistered +0.02 nats/token point-estimate screen. The comparator passed all provenance, three-seed, recipe and compute gates; estimated work differed by -0.003628%.
- **Resource measurements:** GQA used 755,200 parameters / 3,021,056 FP32 tensor bytes versus MHA 820,736 / 3,283,200. After prompts of 32/128/224 tokens and 32 cached decode tokens, actual cache bytes were 131,072/327,680/524,288 versus 262,144/655,360/1,048,576 for MHA. Mean prompt-32 prefill was 13,866 versus 14,570 tokens/sec; cached decode was 591.9 versus 647.1 tokens/sec; training throughput was 29,066 versus 28,604 tokens/sec. Training-process peak RSS means were 671,891,456 versus 666,686,805 bytes. Cached/full logit error stayed below 5.8e-6. These CPU timings and RSS are workload-specific; no energy sensor or CUDA device was available.
- **Interpretation:** This is an eligible, narrow synthetic-corpus comparison, not a broad capability claim or proof of statistically reliable improvement. Three seeds are a minimum; paired NLL SD is 0.024393 nats/token, and the practical threshold was a decision rule rather than a significance test. The quality point estimate is favorable and cache storage is exactly halved, but CPU cached-decode throughput is about 8.5% lower and training-process high-water RSS is slightly higher for GQA. Treat GQA as a quality/cache tradeoff point, not a dominating model. No CUDA or energy measurement was available.
- **Next experiment (updated 23 September 2026):** Q08 CPU accounting and Q09 portable export are implemented. The M2-GQA-002 evidence bundle is committed and verified from a detached clean checkout; bundle SHA-256 is `df79b3578c70c6fa7c3a0d0a2d34e02f257cec16c14295287d73912d2b9bef3a`. Freeze the corpus and task-evaluation protocol in Q10/Q11 before any new architecture learning comparison. Correctness-only sequence tests may proceed, but do not spend confirmation compute until the new evaluation domain is fixed.
- **Evidence follow-up:** A sanitized portable bundle is tracked at [`results/m2-gqa-002/`](results/m2-gqa-002/); its bundle SHA-256 is `df79b3578c70c6fa7c3a0d0a2d34e02f257cec16c14295287d73912d2b9bef3a`. The exporter re-ran the seeded comparator from the six included run directories and preserved the source report's eligibility only after exact agreement on seed membership, eligibility, checks, per-seed deltas, aggregates, scope and provenance. This verifies the comparison record; it does not alter the interpretation or establish an improvement.

## E11-SMOKE-001 — Clean-environment fixture reproduction

- **Status:** Completed 23 September 2026; Windows 11 CPU FP32, Python 3.12.14, PyTorch 2.14.0+cpu, NumPy 2.5.3, clean revision `74bbe3d28f3f80a00fac3a03aac0108036cf4cdd`.
- **Hypothesis:** Two fresh executions of the checked-in deterministic smoke recipe in a clean repository environment reproduce the final model state and fixture metrics.
- **Change:** Run `configs/smoke.json` twice with distinct output directories in the repository's documented editable Python environment. Use the built-in deterministic test fixture; do not acquire a corpus.
- **Control:** The first execution is the control for an exact second execution using the same config, seed, source revision, CPU device, precision, and software environment.
- **Training budget:** 30 optimizer updates and 7,680 input tokens per execution. The preregistration quoted approximately 1.427 billion `decoder-dense-v1` FLOPs based on the older M1 smoke record; both new run summaries instead report 236,666,880 MACs / 1,420,001,280 estimated FLOPs per execution under `decoder-module-mac-v1`, 2,840,002,560 total. This estimator discrepancy is disclosed; the paired runs use identical recorded work. This is an infrastructure reproduction, not a capability comparison.
- **Evaluation metrics:** Final train loss, fixture validation NLL/perplexity, consumed tokens and updates, model-state tensor equality and digest, process-memory scope, and run completion status.
- **Result:** Both runs completed 30 updates / 7,680 tokens with the same schema-1 fixture manifest SHA-256 `c70464681116bd8958c81710cb20768f06a82769d0654382a861960e8eaa9611` and tokenizer artifact SHA-256 `7372690364e84d8c248945180d3c440742725dd79ac27f8f6bada3b7fd912687`. Final train loss was 4.678186893; validation NLL 4.824337328, perplexity 124.503935826 and bits/byte 6.957375728 on 2,180 targets / 2,176 scored UTF-8 bytes. Model-state digest was `f0b866eb45b250b22508937a592510e820ce71ebc04860c48fdae8c8c0673e9b` and `weights.pt` SHA-256 was `43c47f2ef5785ce2e45ccff73bc9b8955a15a90373d11cf7955717798bc45fe7` in both runs; evaluation and final metric records matched exactly. Local ignored run records: [run A summary](runs/q09-clean-repro-a/summary.json), [run B summary](runs/q09-clean-repro-b/summary.json), and their [run A weights](runs/q09-clean-repro-a/weights.pt) / [run B weights](runs/q09-clean-repro-b/weights.pt). The measured process-lifetime RSS peaks were 294,887,424 and 295,759,872 bytes; these are not optimizer-step peaks. CUDA and energy were unavailable.
- **Interpretation:** The clean-environment same-machine CPU smoke path reproduced the model state and fixture metrics exactly on a second fresh execution. This verifies the minimal deterministic training path only; fixture results do not establish language capability, and this check does not reproduce the M2-GQA-002 training comparison. The new checkout had no prepared research corpus or run artifacts before this check, so M2 training was not attempted ahead of Q10 data selection.
- **Next experiment:** Continue Q10.b with an exact-revision article inventory, per-page license/attribution audit, deterministic extraction and train/validation/test preparation before tokenizer fitting.

## Q08-CPU-STEP-001 — Sampled CPU optimizer-step memory probe

- **Status:** Completed 23 September 2026 at clean revision `790a83ac5c60b26ed0bd983d267ae28374488bad`; Windows 11 CPU FP32, Python 3.12.14, PyTorch 2.14.0+cpu.
- **Hypothesis:** Sampling a fresh training worker's current process memory around one normal CPU optimizer step will provide a scoped sampled RSS/private-memory peak and increase that are distinct from the existing process-lifetime high-water reading.
- **Change:** Add a parent-sampled current-memory probe around the production trainer's first optimizer step. The worker is spawned fresh, sets up its model, optimizer and data before the baseline, runs one step through batch sampling, forward/backward, optimizer update and synchronization, then performs normal validation/checkpoint work outside the measured interval. Keep all existing lifetime CPU and per-step CUDA fields unchanged.
- **Control:** The worker's own current RSS and private-memory reading immediately before the step is the within-process baseline. Existing process-lifetime high-water values remain context only and are not used to calculate the step peak or increase.
- **Training budget:** One optimizer update using the deterministic smoke configuration: batch size 4, context length 64, one accumulation, and 256 input tokens. This is a CPU measurement plumbing check, not a model comparison or capability experiment.
- **Evaluation metrics:** Baseline current RSS/private bytes; maximum sampled current RSS/private bytes; increase above baseline; sample count and observed polling gaps; step duration; sensor method; worker/run status; and the recorded measurement scope. CUDA is reported unavailable on this host.
- **Result:** `frontier profile-cpu-step-memory --config configs/smoke.json --output-dir runs/q08-cpu-step-probe --sample-interval-ms 5 --timeout-seconds 120` completed one optimizer update / 256 tokens / 47,333,376 estimated FLOPs. Win32 current-memory baseline was 282,390,528 RSS bytes and 797,462,528 private-commit bytes. The largest of two samples was 291,401,728 RSS bytes (+9,011,200) and 801,554,432 private-commit bytes (+4,091,904). The worker-reported step duration was 0.013064 seconds; requested interval was 5 ms and observed sample gap was 13.189 ms. The run summary records schema 2 `training.memory.cpu_optimizer_step_sampled_peak`, separate from the process-lifetime high-water fields. CUDA was unavailable.
- **Interpretation:** This verifies the fresh-worker handoff, normal trainer step boundary, current RSS/private-memory collection, and persisted measurement record. The 13.189 ms sampling gap was about as long as the 13.064 ms step, so this run had only start and completion-boundary samples and cannot resolve a transient interior peak. Treat the values only as sampled maxima for this step, not an exact CPU peak or a capability/resource comparison. CUDA behavior remains unvalidated.
- **Next experiment:** Continue Q10.b with the exact-revision article inventory and per-page license/attribution audit; keep research training behind the Q10/Q11 data and evaluation gates.

## Q09-REPRO-002 — Independent clean-checkout fixture reproduction

- **Status:** Completed 23 September 2026 from a separate clean clone at revision `790a83ac5c60b26ed0bd983d267ae28374488bad`; Windows 11 CPU FP32, Python 3.12.14, PyTorch 2.14.0+cpu.
- **Hypothesis:** A fresh run of the deterministic CPU smoke recipe from a separate clean clone of the pinned current revision will reproduce the existing fixture checkpoint digest, manifest identity, token/update counts and validation metrics.
- **Change:** Clone the repository locally without sharing writable source files, check out `790a83ac5c60b26ed0bd983d267ae28374488bad`, force Python imports to resolve from the cloned `src/`, then run the checked-in `configs/smoke.json` once to a fresh output directory. Do not change the recipe or dependencies.
- **Control:** Q09.e's first same-environment smoke run at clean revision `74bbe3d28f3f80a00fac3a03aac0108036cf4cdd`, with `weights.pt` SHA-256 `43c47f2ef5785ce2e45ccff73bc9b8955a15a90373d11cf7955717798bc45fe7`, fixture manifest SHA-256 `c70464681116bd8958c81710cb20768f06a82769d0654382a861960e8eaa9611`, train loss `4.678186893`, and validation NLL `4.824337328`.
- **Training budget:** One 30-update / 7,680-token smoke execution; approximately 1,420,001,280 estimated FLOPs under `decoder-module-mac-v1`. This reproduces an infrastructure fixture only and does not test capability or reproduce M2-GQA-002.
- **Evaluation metrics:** Run completion, Git revision/clean status recorded in the run environment, data manifest and tokenizer hashes, model/checkpoint digest, final train loss, validation NLL/perplexity, consumed tokens and optimizer updates.
- **Result:** The separate clone was clean at the pinned revision and `frontier` imported from its `src/`. The fresh run completed 30 updates / 7,680 tokens with the same fixture manifest SHA-256 `c70464681116bd8958c81710cb20768f06a82769d0654382a861960e8eaa9611`, tokenizer SHA-256 `7372690364e84d8c248945180d3c440742725dd79ac27f8f6bada3b7fd912687`, final train loss `4.67818689346313`, validation NLL `4.824337328464613`, perplexity `124.50393582591181`, and `weights.pt` SHA-256 `43c47f2ef5785ce2e45ccff73bc9b8955a15a90373d11cf7955717798bc45fe7`. All compared values match Q09.e; the run output remains in the separate local clone and is not tracked.
- **Interpretation:** The clean-checkout run reproduces the bounded fixture on the same host and software environment. It validates checkout/import/reproduction plumbing only; it does not establish cross-machine determinism or independently reproduce the three-seed M2-GQA-002 comparison. No capability claim is made.
- **Next experiment:** Continue Q10.b with exact-revision source inventory and per-page license/attribution screening before fetching text. Keep Q11 task/scorer, contamination, and conventional baseline gates ahead of sequence-block training.

## Q10-PREP-001 — Deterministic held-out corpus preparation contract

- **Status:** Completed 23 September 2026 as a bounded data-pipeline package in the new checkout based on `7804dff`.
- **Hypothesis:** A deterministic three-way split that records normalized-document identities can freeze training, validation and test membership while exposing duplicate groups and retaining the prior two-way workflow.
- **Change:** Add opt-in schema-4 `test.txt` output, deterministic largest-remainder split allocation with at least one unique document in each split, SHA-256 identity lists, and a duplicate report that retains source rows while assigning normalized duplicates together. Add `load_all_splits` as the explicit test-set access path. Preserve schema-3 behavior when `test_fraction` is zero.
- **Control:** The existing train/validation preparation path with `test_fraction=0` remains the compatibility control; the pre-existing repeatability and normalized-duplicate tests continue to exercise it.
- **Training budget:** None. No corpus was acquired, no model was trained, and no optimizer updates or token/FLOP budgets were consumed.
- **Evaluation metrics:** Repeatability of the complete schema-4 manifest, distinct split identity hashes, duplicate group/row counts, train/validation/test content-to-manifest checks, held-out-content tamper rejection, CLI wiring, repository tests and style checks.
- **Result:** The focused data/evaluation suite passed 25 tests. The full suite passed 106 tests in 33.88 seconds; Ruff check passed; Ruff format check reported 50 files already formatted; `frontier verify-evidence --bundle-dir results/m2-gqa-002` returned `verified` with eight files and bundle SHA-256 `df79b3578c70c6fa7c3a0d0a2d34e02f257cec16c14295287d73912d2b9bef3a`; `frontier prepare-data --help` exposed `--test-fraction`; `git diff --check` was clean.
- **Interpretation:** The splitter contract is implemented and validated, but this does not establish corpus suitability, provenance, licensing, tokenizer fit, task validity or language-model capability. No Wikipedia revision inventory is frozen and no text has been fetched. The direct API metadata endpoint could not be reached from the restricted checkout, so no revision IDs were guessed.
- **Next experiment:** Freeze a bounded exact-revision candidate inventory from primary Wikimedia sources and audit each page's footer, history and discussion for applicable license, imported text, exceptions and attribution. Fetch no revision body until the inventory and per-page checks are recorded. Then perform the two identical preparations required by Q10.b before tokenizer fit.

## Q10-SOURCE-001 — Metadata-only Wikipedia revision inventory (preregistered)

- **Status:** Metadata inventory and first-pass rights screen completed 23 September 2026. The candidate list and stop rules remain frozen in [`docs/q10b_wikipedia_candidate_inventory.json`](docs/q10b_wikipedia_candidate_inventory.json); the body-acquisition gate did not pass.
- **Hypothesis:** A fixed, topic-stratified title list can produce a bounded exact-revision inventory with sufficient primary-source license and attribution evidence before corpus text is acquired.
- **Change:** Query metadata only for the 24 frozen English Wikipedia titles; record page/revision IDs, timestamp, byte size, SHA-1, content model, canonical title and oldid permalink. Require at least 20 eligible pages, no revision above 1 MiB, and no more than 4 MiB combined. Screen exact-oldid footers, up to 500 history entries per page, discussion pages, and metadata-only template names; record unresolved source chains and exclusions.
- **Control:** The frozen candidate list and predeclared metadata, size and rights criteria control selection. This is a corpus-provenance gate, not a model comparison; no capability control is applicable.
- **Training budget:** Zero model updates, training tokens, or training FLOPs. No raw revision-content API field was requested, and no corpus text was saved or prepared.
- **Evaluation metrics:** Candidate count; metadata completeness; metadata/size-eligible count; per-page and aggregate revision byte sizes; full-history coverage; license/attribution dispositions; revision-inventory SHA-256; and whether corpus text was acquired.
- **Result:** All 24 titles resolved to namespace-0, non-redirect, main-slot wikitext revisions. Each revision was below 1 MiB; the largest was 331,340 bytes and the combined size was 3,272,433 bytes. Revision-row SHA-256: `150db1a04035428e84a298f02957fd685f724b3a29cbfe2692106e63fc1b0a7d`. Exact-oldid footers reviewed by the page auditors showed CC BY-SA 4.0 with “additional terms may apply.” The audit covered at most the newest 500 history entries per page and current talk-page material, not each page’s full history. It found copied/imported-text chains, two suspected-AI-content holds, and additional open-license attribution notices; source mapping and history coverage remain incomplete. **Zero pages are cleared for acquisition under the current evidence standard.** No raw revision body was requested through the API and no corpus text was saved; rendered oldid pages were opened only for footer review.
- **Interpretation:** Metadata and size rules pass, but the source-rights and attribution gate does not. A bounded recent-history scan cannot clear a page when older history remains unchecked, and imported/open-license text requires traceable source and attribution records. The source terms do not expressly state an AI-training grant; treating CC BY-SA reuse as sufficient for local research remains an interpretation recorded in `docs/data_protocol.md`. No corpus or language-capability evidence exists.
- **Next experiment:** Preserve this stop result. Complete source attribution and history review or preregister an amended candidate set with primary-source evidence; request no corpus text until at least 20 pages pass every rule.

## Q08-CPU-STEP-002 — Longer-window sampled CPU optimizer-step probe

- **Status:** Preregistered at `ca5674560933ff5fd6e9e69f5bd9e74a78f5aca0` and completed from clean revision `53e9ed018a7c4bc755b4cb9286248ed0881b33e8` on 23 September 2026; Windows 11, Python 3.12.14, PyTorch 2.14.0+cpu.
- **Hypothesis:** Extending one normal CPU optimizer step with gradient accumulation will give the parent sampler enough time to capture interior RSS/private-memory samples while leaving the smoke model and each microbatch unchanged.
- **Change:** Run the production trainer through `frontier profile-cpu-step-memory` using `configs/smoke.json` with only `train.gradient_accumulation` changed from 1 to 16. Keep seed 17, model architecture, batch size 4, context length 64, optimizer, data, precision and determinism fixed. Use a unique ignored output directory, a 5 ms requested sample interval and a 120 second timeout. Record the effective config hash and all sample gaps.
- **Control:** Q08-CPU-STEP-001 is the sampling-path reference at one accumulation step; it is not a resource comparison. This probe makes no baseline/candidate or capability claim.
- **Training budget:** One CPU optimizer update, 16 microbatches, 4,096 input tokens and 757,334,016 estimated FLOPs (`decoder-module-mac-v1`); no multi-step or research-corpus training.
- **Evaluation metrics:** Current RSS and private-memory baseline; maximum sampled values and increases; sample count, minimum/mean/maximum gaps, step duration, worker/run status, effective config hash, output revision/dirty state, sensor method and CUDA availability.
- **Result:** `frontier profile-cpu-step-memory --config runs/q08-cpu-step-probe-accum16/probe.json --output-dir runs/q08-cpu-step-probe-accum16/worker-run --sample-interval-ms 5 --timeout-seconds 120` completed one CPU update / 4,096 tokens / 757,334,016 estimated FLOPs. Effective config SHA-256: `20773d83b308211f0f233849f1f3180dc06f67139b579bb828aacb2c3941b07e`. Win32 baseline was 282,738,688 RSS / 797,708,288 private-commit bytes. Across 11 samples, maxima were 291,721,216 RSS / 801,230,848 private bytes, increases of 8,982,528 / 3,522,560 bytes. The synchronized step took 0.151018 seconds; requested interval was 5 ms and observed gaps were 9.457–17.097 ms (mean 15.113 ms). The run summary records the clean source revision and `git_worktree_clean: true`; CUDA was unavailable. The broader invocation's process-lifetime high-water reading was 292,945,920 RSS / 801,501,184 private bytes and is not the step measurement.
- **Interpretation:** The longer step produced repeated samples during its interior, so the run resolves the earlier two-boundary-sample limitation for this CPU workload. It still reports a sampled maximum, not an exact peak, and can miss allocations between readings. Do not compare absolute memory increases with Q08-CPU-STEP-001: the baselines differ, and this probe uses 16 microbatches. No architecture, capability or general resource claim follows.
- **Next experiment:** Close this bounded CPU probe, record CUDA as unavailable on this host, and return to Q10 source eligibility. Do not acquire article bodies or begin model training while fewer than 20 candidate versions pass the rights and attribution gate.

## Q08-CPU-STEP-003 — One-millisecond requested sampling follow-up

- **Status:** Completed 24 September 2026 from clean revision a8bf3afd79c9a4ce896b3e22501799a090b02bc1; Windows 11, Python 3.12.14, PyTorch 2.14.0+cpu. This is a bounded diagnostic follow-up, not a capability comparison.
- **Hypothesis:** Requesting a 1 ms interval will increase interior CPU memory sample coverage over Q08-CPU-STEP-002's 5 ms request.
- **Change:** Repeat the same one-update, 16-microbatch smoke probe from runs/q08-cpu-step-probe-accum16/probe.json with a 1 ms requested interval and fresh output directory runs/q08-cpu-step-probe-1ms. The effective resolved-config SHA-256 is 3bea3233279b2d76b935265c78572302b06b31ee2eb5f8235257f2fa42f97f7b.
- **Control:** Q08-CPU-STEP-002 used the same effective training workload with a 5 ms requested interval. The two probes characterize sampling only; they are not resource comparisons between models.
- **Training budget:** One CPU update, 16 accumulation microbatches, 4,096 tokens and 757,334,016 estimated FLOPs under decoder-module-mac-v1. No research-corpus training.
- **Evaluation metrics:** Requested interval, sample count, observed gaps, current RSS/private-memory baseline and sampled maxima/increases, step duration, resolved-config hash, source revision/clean status, sensor and CUDA availability.
- **Result:** The run completed one update in 0.073629 seconds and collected six samples. The baseline was 282,095,616 RSS / 796,856,320 private bytes; sampled maxima were 291,262,464 / 800,391,168 bytes (increases 9,166,848 / 3,534,848). Observed gaps were 12.624–16.019 ms (mean 14.736 ms), despite the 1 ms request. The run environment records clean revision a8bf3afd79c9a4ce896b3e22501799a090b02bc1 and CUDA unavailable. The local ignored output is runs/q08-cpu-step-probe-1ms.
- **Interpretation:** The 1 ms request did not produce denser observations than the earlier 5 ms probe; this step was shorter and yielded six samples, with gaps still around 13–16 ms. The maximum remains sampled and can miss a transient. Process-lifetime RSS is separate from the optimizer-step reading. No exact CPU transient peak or CUDA measurement is available.
- **Next experiment:** Keep E08 partial. Do not continue interval tuning on this host; validate CUDA behavior only on a suitable device and revisit exact CPU transient measurement if a higher-resolution supported sensor becomes available.

## Q09-REPRO-003 — Bounded real-corpus seed-17 screen rerun

- **Status:** Completed 24 September 2026 at clean revision a8bf3afd79c9a4ce896b3e22501799a090b02bc1; Windows 11 CPU, Python 3.12.14, PyTorch 2.14.0+cpu. The machine-readable result is [Q09-REPRO-003 result v1](docs/q09_repro003_seed17_screen_result_v1.json).
- **Hypothesis:** The fresh checkout can reconstruct the exact pinned TinyStories split and reproduce the earlier seed-17 5%-budget GQA screen metrics using the current committed training code.
- **Change:** After checking the pinned [TinyStories dataset card](https://huggingface.co/datasets/roneneldan/TinyStories/blob/f54c09fd23315a6f9c86f9dc80f725de7d8f9c64/README.md) and the [CDLA-Sharing 1.0 agreement](https://cdla.dev/sharing-1-0/), retrieve only bytes 0–20,971,519 from the pinned training file. Verify raw-prefix SHA-256 3232df12d6bdd8f83071f0d8196ad10128624168e228aaf9a23f6a1f8431ee8a, then reconstruct the deterministic schema-3 split and run the existing seed-17 screen configs in fresh output directories. The first current-schema preparation wrote LF split files and therefore did not match the historical file hashes. The mismatch was traced to the post-M2 schema-4 change to byte-oriented split writing and the source basename: preserving the original source basename and Windows CRLF split representation reproduced the historical train, validation and manifest hashes exactly. The LF diagnostic output is retained locally under ignored data.
- **Control:** A fresh conventional MHA-A run from configs/m2/tinystories_v3_mha_screen.json; pair it with the fresh GQA run from configs/m2/tinystories_v3_gqa_screen.json at seed 17. The prior M2-GQA-001 screen is a rounded metric reference only; its checkpoint artifacts are not present in this checkout.
- **Training budget:** MHA used 51 updates / 104,448 tokens / 677,905,956,864 estimated FLOPs. GQA used 54 updates / 110,592 tokens / 674,296,233,984 estimated FLOPs, a -0.53248% candidate delta. Pair total: 1,352,202,190,848 estimated FLOPs. This is the existing 5%-budget screen, not the full M2-GQA-002 budget.
- **Evaluation metrics:** Full TinyStories validation mean NLL, perplexity and UTF-8 bits/byte; scored token/byte counts; data manifest, split and tokenizer hashes; consumed tokens, estimated FLOPs, run completion, weights hashes, process-lifetime RSS and clean source revision. No inference profile or isolated CPU optimizer-step measurement was repeated.
- **Result:** Both runs completed with no failed or omitted run. The exact training manifest SHA-256 was 6ad63d98d5fb05682bb1cb929d8224b298a792ab418852a3e851c4d8d4a98870; prepared-manifest SHA-256 was 5bac1fc6d9ab7af5ed4c9a61d3a5ccbd2426faebb75acf2b605b013e146841ac; train/validation hashes were 87ac33a8f47aeefdf772bd813472976eede4543d98d2c4122a1b07271163b44e / 0ad8843aee6aff10a8bfc58fdfd80e26b261d061f341dffb12894a538f18a935; tokenizer SHA-256 was 7372690364e84d8c248945180d3c440742725dd79ac27f8f6bada3b7fd912687. MHA scored NLL 3.658962205, perplexity 38.821033683 and 5.275252372 bits/byte; GQA scored NLL 3.556165681, perplexity 35.028628376 and 5.126583100 bits/byte. The one-seed GQA-minus-MHA NLL delta was -0.102796524 nats/token. These values reproduce the earlier screen metrics to the precision previously recorded. Per-arm summaries: [MHA](runs/q09-m2-seed17-screen-rerun/mha-seed17/summary.json) and [GQA](runs/q09-m2-seed17-screen-rerun/gqa-seed17/summary.json).
- **Interpretation:** The pinned source, prepared data, tokenizer, seed-17 training budgets and rounded screen metrics reproduce from the new checkout. This is one paired seed on synthetic TinyStories, not an eligible three-seed claim and not an independent rerun of the full six-run M2-GQA-002 confirmation. The prior screen checkpoints were absent, so their weight digests could not be compared. Process-lifetime RSS is not an optimizer-step peak; CUDA and energy are unavailable.
- **Next experiment:** Keep sequence-module training gated. A full independent M2-GQA-002 rerun requires all three full-budget pairs (81,666,247,753,728 estimated FLOPs total); it exceeds this bounded check. If that full rerun is required before S04, complete it on one clean pinned revision with all failures retained. Otherwise keep the limitation explicit and do not describe this screen as the full rerun.

## Q10-SOURCE-002 — PMC CC BY 4.0 candidate metadata screen (preregistered)

- **Status:** Complete, source gate failed (23 September 2026). Candidate selection was preregistered in clean commit `b5e729c8dcb5008df925bd8832c6d2a2fbc9ae80`; the rights/date clarification was committed in `4dcab13f89cce4cfa24d80e0adb2331abf030530` before screening candidates 2–24. All 24 frozen records were screened. Five pass the metadata rules (547,447 combined XML bytes); 19 fail. The minimum is 20, so the body-request gate remains closed. No article XML, full text, figure or supplement has been requested.
- **Hypothesis:** The PMC Open Access Subset's per-version machine-readable license and checksum metadata can identify a bounded scientific-text set with at least 20 exact CC BY 4.0 versions within the size cap before text acquisition.
- **Change:** Supersede the active Wikipedia candidate search with the six-stratum protocol in [`docs/q10c_pmc_candidate_inventory.json`](docs/q10c_pmc_candidate_inventory.json). Freeze 24 unique PMCID candidates by numeric query order before inspecting their metadata; do not replace an ineligible candidate. Keep Q10-SOURCE-001 and its zero-pass result unchanged as historical evidence. Require one unambiguous Cloud version, a CC BY family `license_code`, at least one OAI `dc:rights` entry exactly equal to the CC BY 4.0 URI, no conflicting license URI/restriction, in-range and consistent 2018–2025 publication dates, version flags, XML presence and size. Article bodies, figures and supplements remain gated.
- **Control:** Q10-SOURCE-001 is the failed source-screen reference (0/24 Wikipedia pages cleared under the documented full-history standard); it is not a model or capability control. No comparative capability claim is planned here.
- **Training budget:** Zero article bodies, prepared corpus bytes, training tokens, updates or FLOPs. Metadata queries only; do not request article XML unless all 24 candidate IDs are frozen, at least 20 versions pass, and the combined size of all passing XML objects is within 4 MiB.
- **Evaluation metrics:** Per-stratum query counts and candidate IDs; version count; raw license code and exact OAI rights; source metadata completeness; XML object sizes and MD5 values; eligible count; combined eligible XML size; and whether any article body was requested. Retain only OAI Dublin Core fields needed for identity/citation/rights, and discard any `dc:description` that contains abstract text.
- **Result:** Six queries returned counts of 985, 8,391, 25,007, 2,091, 58,882 and 713,404; the capped ID lists, query strings, timestamps and hashes are stored in the inventory. The frozen cohort produced 5 eligible versions totaling 547,447 XML bytes and 19 ineligible candidates. Failures include 12 manuscript flags, 10 date-window/consistency failures, 5 ambiguous multi-version records and 1 CC BY 2.5 record; categories overlap. The five eligible IDs are PMC5798820, PMC5802623, PMC11261691, PMC11261710 and PMC11281480. The inventory records exact XML keys, sizes, MD5 values and S3 modification times. No OAI abstract description was retained, and no article XML, body, figure or supplement was requested.
- **Interpretation:** The preregistered minimum of 20 eligible versions was not met, so PMC is a failed source candidate under this bounded screen. The failures are mostly manuscript-flag and date/version eligibility problems, not evidence that all PMC material is unusable. The planned evidence scope would have been English MEDLINE-indexed biomedical/life-sciences prose, not general English capability. Even a metadata pass would still require article-level rights review before preparation.
- **Next experiment:** Q10-SOURCE-003 preserved a failed six-query attempt where ESearch misread the NOT exclusions and returned zero IDs. Preregister a corrected grouped-Boolean attempt as Q10-SOURCE-004 before sending any replacement query; keep the failed attempt unchanged.

## Q10-SOURCE-003 — PMC published-article metadata screen (preregistered)

- **Status:** Completed with a query-syntax failure (23 September 2026). The protocol was preregistered against parent revision `92ddd131336533412e2a7345e51672383ee8836e`; all six ID-only query records are retained in the inventory. ESearch returned zero IDs in all six strata because it translated the attempted `AND NOT` exclusions as positive AND terms. No candidate metadata or article content was requested.
- **Hypothesis:** Excluding PMC author manuscripts and preprints at search time, then screening 48 unique IDs, can identify at least 20 published journal-article versions with exact CC BY 4.0 rights and no more than 4 MiB combined XML before content acquisition.
- **Change:** Create a new six-stratum cohort of eight unique candidates per stratum. Add `NOT "author manuscript"[filter] AND NOT preprint[filter]` to the prior English, publication-date, CC BY and MeSH query. Keep the original Q10-SOURCE-002 cohort and its 5/24 result closed; do not substitute its candidates. The PMC User Guide documents both publication-stage filters.
- **Control:** Q10-SOURCE-002 is the failed metadata-screen reference (5/24 eligible, 547,447 XML bytes); it is a source-selection control, not a model or capability control.
- **Training budget:** Zero article bodies, prepared corpus bytes, training tokens, updates or FLOPs. Issue only six capped ID-list queries and then bounded metadata requests. No article XML unless all 48 candidates are screened, at least 20 pass, and eligible XML totals no more than 4 MiB.
- **Evaluation metrics:** Query counts and full returned ID lists; candidate-list hashes; publication-stage/version flags; exact rights, date and citation fields; per-object MD5, modification time and bytes; eligible count and aggregate bytes; body-request status.
- **Result:** All six frozen queries returned count 0 and list length 0. Each translation joined `"author manuscript"[Filter]` and `"preprint"[Filter]` with positive AND terms and emitted the warning `NOT` / `No items found.` The candidate selection gate failed at 0/48. The inventory preserves each query string, timestamp, translation, warning list and empty-list hash. No per-version metadata, article XML, figures, supplements or prepared text were requested.
- **Interpretation:** The syntax failure does not show that PMC lacks eligible published articles. It shows that ESearch did not apply the exclusion clause written as `AND NOT ... AND NOT ...`. The candidate source, rights rules and cohort were not evaluated because no IDs were selected.
- **Next experiment:** Q10-SOURCE-004 records a separate pre-query correction using the PMC-documented grouped form `(positive terms) NOT ("author manuscript"[filter] OR preprint[filter])`. It adds an ESearch query-translation gate and starts a new six-query attempt; no result from this zero-ID attempt is reused.

## Q10-SOURCE-004 — PMC corrected published-article metadata screen

- **Status:** Complete; metadata gate passed, article XML not yet requested (23 September 2026). Preregistered in `docs/q10e_pmc_corrected_candidate_inventory.json` at commit `a58903f9c5f40012a97368db394ce14704ab1c87` against parent revision `0a2cc5d24bc6ae93185340f55a56289dcd81e4cb`. A local PowerShell parse error occurred before any network request and left the inventory unchanged; the command was corrected before the first ESearch.
- **Hypothesis:** Grouping positive PMC filters before one terminal Boolean `NOT` clause will return the intended published-article candidate sets while excluding author manuscripts and preprints.
- **Change:** Keep the Q10-SOURCE-003 scope, six MeSH strata, 48 candidate target, date and CC BY filters, order rules, metadata criteria and limits; correct only the Boolean expression to `(<positive terms>) NOT ("author manuscript"[filter] OR preprint[filter])`. Before continuing to another query or selecting IDs, confirm ESearch's query translation preserves that exclusion and emits no standalone `NOT` warning. Q10-SOURCE-003 remains a separate failed syntax record with zero IDs.
- **Control:** Q10-SOURCE-003 is the same-scope syntax-failure reference (six zero-result ID queries); it is not a data or capability control.
- **Training budget:** Zero article XML/body bytes, prepared corpus bytes, training tokens, updates or FLOPs. Six capped E-Utilities ID queries and metadata-only screening for the 48 frozen candidates were performed. The preregistered body gate required all candidates screened, at least 20 passing, and eligible XML totaling no more than 4 MiB; the gate passed, but no XML has been fetched yet.
- **Evaluation metrics:** ESearch translation and warnings; per-stratum result counts, original-order ID lists and hashes; 48-candidate selection digest; version flags; exact rights, date and citation fields; per-object MD5, modification time and bytes; eligible count and aggregate bytes; body-request status.
- **Result:** All six translations preserved the final `NOT ("author manuscript"[Filter] OR "preprint"[Filter])` clause and had no standalone `NOT` warning. ESearch counts and returned-list lengths were neuroscience 961/961, microbiology 8,368/1,000, plants 24,970/1,000, ecology 2,080/1,000, computational biology 58,571/1,000 and public health 710,139/1,000. The inventory freezes 48 unique IDs with selection SHA-256 `8489310e65e4b0c65ffc1f59afdc93dd5ac841ddbe306294a5d8205c9a843adb`. Metadata screening found 21 eligible and 27 ineligible records, with no request errors. Eligible counts were neuroscience 5/8, microbiology 8/8, ecology 8/8, and 0/8 in plants, computational biology and public health. Eligible XML object metadata totals 3,153,456 bytes (maximum 668,319 bytes per object). The 27 exclusions comprise 24 multiple-version records, three date inconsistencies/out-of-range records and one conflicting license record; the date and license categories overlap. No article XML, abstract description, figure, supplement or prepared text was requested. Exact per-query lists, hashes, rights, version flags and object metadata are in `docs/q10e_pmc_corrected_candidate_inventory.json`.
- **Interpretation:** The corrected Boolean syntax retrieved the intended published-stage-filtered candidate sets, and the frozen minimum-20 / maximum-4-MiB metadata gate passed. This only authorizes retrieving the 21 metadata-eligible XML objects for a separate article-level rights audit. It does not establish that 20 documents remain usable after that audit, that the resulting corpus is useful, or that a model capability claim generalizes beyond this MEDLINE-indexed biomedical/life-sciences scope.
- **Next experiment:** Retrieve only the 21 eligible XML objects serially through an approved PMC service. Inspect each article's license statement and separately licensed text/media notices; exclude untraceable or conflicting material. Continue to deterministic preparation only if at least 20 complete documents remain eligible.

## Q10-SOURCE-005 — PMC article-level license and rights audit (source gate failed)

- **Status:** Preregistered before article XML access at revision `dd6863cf1a6c7def5854c90d816dc0aa3c728bb`. The 21-object allowlist, integrity evidence, original literal-HTTPS criterion and Q10-SOURCE-006 amendment are in `docs/q10f_pmc_article_rights_inventory.json`. Retrieval passed; the frozen minimum-document gate failed under the amended explicit-href rule.
- **Hypothesis:** At least 20 of the 21 metadata-eligible article versions will carry an explicit article-level CC BY 4.0 license and contain no untraceable third-party prose that prevents deterministic preparation.
- **Change:** After rechecking current PMC service guidance, use only the current PMC Cloud service via serial HTTPS GETs for the 21 exact versioned XML keys. Verify each response against its frozen size and MD5, parse the JATS permissions/license statement, confirm the article identity, and exclude separately licensed or unclear body text. Do not request TXT, PDF, figures, supplements, media or alternate versions; preserve all XML and corpus text outside Git.
- **Control:** Q10-SOURCE-004 is the frozen metadata-screen gate (21/48 eligible, 3,153,456 expected XML bytes); it is a source-selection reference, not a data-quality or capability control.
- **Training budget:** Zero training tokens, updates or FLOPs. The preregistered 21 serial requests retrieved exactly 3,153,456 XML bytes, each object no larger than 1 MiB, with one request in flight and at least 500 ms between requests. All objects passed frozen size and MD5 checks; SHA-256 values are recorded. Raw XML remains ignored under `data/`.
- **Evaluation metrics:** Download success, exact object size, MD5 and SHA-256; XML PMCID/title/DOI identity; article-level license href/text; conflicting or third-party rights notices; rights-clear document count; bytes and failures. Keep article text and raw XML out of Git.
-- **Result:** All 21 frozen XML objects were retrieved serially from current PMC Cloud HTTPS URLs and passed expected byte-length and MD5/ETag checks. Total bytes were 3,153,456; each SHA-256 and request record is in the inventory. A secure parse of the exact JATS `front/article-meta/permissions/license` subtree and its descendant XLink hrefs found matching PMCID/version/title/DOI, canonical HTTPS OAI rights, and a CC BY 4.0 license-text signal for 21/21. Thirteen records contain one of the exact href forms permitted by Q10-SOURCE-006. Eight records—PMC11261691, PMC11261710, PMC11281480, PMC11281800, PMC11288634, PMC11316580, PMC11349053 and PMC11350378—contain a canonical ALI `license_ref` and matching license statement but no qualifying href attribute. Under the frozen rule, at most 13 records could pass the later body-rights screen, below the 20-document minimum. The cohort stopped before that screen; zero documents are fully rights-cleared. No article prose was extracted, no candidate was replaced and training tokens/updates/FLOPs are zero.
- **Interpretation:** PMC passed the bounded retrieval-integrity check but failed the preregistered minimum-document source gate. The eight missing-href records are protocol-ineligible under this screen; this does not establish that those articles are unlicensed. The other 13 are not rights-cleared because body-level third-party-rights review was not run after the minimum became unattainable. Preserve this negative result without retroactively treating ALI `license_ref` text as an href.
- **Next experiment:** Q10-SOURCE-007 is preregistered as a separate re-audit of this same frozen 21-object cohort under the authoritative NISO ALI `license_ref` representation. Keep this SOURCE-005/006 explicit-href failure intact; add no candidates and make no additional requests.

## Q10-SOURCE-006 — PMC CC BY 4.0 URI scheme normalization amendment (applied; minimum gate failed)

- **Status:** Frozen on 23 September 2026 before cohort rights decisions and applied without changing its rule. This is a protocol amendment to Q10-SOURCE-005, not a retroactive change to its original criterion.
- **Hypothesis:** A narrow normalization of the two exact CC BY 4.0 HTTP/HTTPS URI forms will avoid rejecting a license solely because the JATS link uses HTTP, while corroborating article text and frozen OAI rights continue to fail closed on mismatches.
- **Change:** Preserve SOURCE-005's literal HTTPS rule as the original record. For the cohort screen, accept only the exact href `http://creativecommons.org/licenses/by/4.0/` or `https://creativecommons.org/licenses/by/4.0/`, with no port, query, fragment, credentials or path variation. Preserve the raw URI and normalize the HTTP form to canonical HTTPS only for identity comparison. Also require article text identifying CC BY 4.0, an exact canonical HTTPS URI among the frozen OAI rights values, and no conflicting license or restriction. Fail missing, conflicting or unclear evidence. The pilot parsed one local XML with DTD processing ignored and external resolution disabled; it observed an HTTP href and matching HTTPS license-text prefix plus OAI URI, without extracting article prose.
- **Control:** Q10-SOURCE-005's frozen literal-HTTPS criterion and its pre-audit state. This protocol comparison is not a capability control.
- **Training budget:** Zero corpus text, tokens, updates or FLOPs. No additional network requests. One local license-element pilot informed the amendment; the entire allowed cohort was then checked for identity and explicit license hrefs, but no full body-rights review or eligibility clearance was performed.
- **Evaluation metrics:** Raw and canonicalized URI; exact XML/OAI/license-text agreement; identity checks; conflicting license and third-party notices; complete rights-cleared document count; extraction eligibility; bytes and exclusion reasons.
- **Result:** The pilot on PMC5798820 version 1 found an HTTP `href`, a license-text URL using canonical HTTPS, and canonical HTTPS among frozen OAI rights. After the amendment was frozen, the secure cohort screen found 13/21 records with a permitted exact XLink href and eight with only ALI `license_ref` text and no href. Identity, OAI and license-text signals matched in 21/21. Because at most 13 could pass the full audit, below the frozen minimum of 20, the screen stopped before examining body-level third-party notices. The [official CC BY 4.0 deed](https://creativecommons.org/licenses/by/4.0/) resolves the HTTP reference to a page that identifies HTTPS as canonical; the [legal code](https://creativecommons.org/licenses/by/4.0/legalcode) confirms that canonical URL. This inference concerns URI identity only, not article-content rights.
- **Interpretation:** The narrow scheme normalization worked for hrefs present in the JATS license subtree, but it did not make the cohort eligible: eight records lack an explicit href and the minimum cannot be met. This remains a method-gate failure, not a legal conclusion that the ALI references or articles are unlicensed. The 13 records with hrefs remain uncleared. A subsequent primary-standard check found that JATS and NISO define `ali:license_ref` as a URI pointer to license terms and JATS best practice omits `xlink:href` when that element is used; the separate Q10-SOURCE-007 experiment will test that format prospectively without replacing this result.
- **Next experiment:** Apply the separately frozen Q10-SOURCE-007 protocol to the same 21 XML files. Preserve the SOURCE-005/006 results and do not add candidates or make further requests.

## Q10-SOURCE-007 — PMC rights re-audit with NISO ALI license_ref support (completed; minimum gate passed)

- **Status:** Preregistered on 23 September 2026 at clean revision `0f2389bb1771c18f714d9256b292579699cf7c3b` (prepared from parent `e3506d99ac653e4315f8ed0130f9458ad395c832`). The exact 21-record input and protocol are in `docs/q10g_pmc_ali_rights_audit.json`. The rights review began from clean revision `afd2a5c5700b266be05d6f80b6817f8a5b23646d`; Q10-SOURCE-005/006 results remain unchanged.
- **Hypothesis:** At least 20 of the 21 frozen articles will satisfy the full rights audit when the JATS-standard NISO ALI `license_ref` is accepted as an alternative URI pointer, and no untraceable third-party prose will prevent deterministic extraction.
- **Change:** Accept either (1) an exact Q10-SOURCE-006 XLink href inside the article-level `front/article-meta/permissions/license` subtree, or (2) a correctly namespaced NISO ALI `license_ref` element within that subtree whose trimmed text is exactly the canonical HTTPS CC BY 4.0 URI. Require exact XML identity, exact canonical HTTPS in frozen OAI rights, a consistent article license statement, and no conflicting or unclear rights affecting abstract/main-body prose. Screen included prose in memory for rights notices, retain classifications only, and preserve the earlier SOURCE-005/006 results unchanged.
- **Control:** Q10-SOURCE-005/006's explicit-href-only protocol on the same 21 XML files (13 hrefs, eight ALI-only references, and zero full rights clearances). This is a protocol control, not a data or capability control.
- **Training budget:** Zero additional network requests, prepared or retained corpus text, training tokens, updates or FLOPs. Article abstract/main-body text was read transiently in memory for the required rights review and was not saved as a corpus. The 21 XML inputs were already retrieved under SOURCE-005 and all byte lengths, MD5 values and SHA-256 values were reverified.
- **Evaluation metrics:** PMCID/version/title/DOI identity; XLink or ALI pointer type/value/namespace; license-statement and OAI consistency; conflicting license and restriction scope; rights-relevant body notices; fully rights-cleared document count; exclusions and reasons; retained-text status.
- **Result:** All 21/21 exact XML inputs passed size, MD5, SHA-256 and identity checks. All 21 have exactly one article-level JATS license element with the exact, correctly namespaced NISO ALI canonical HTTPS CC BY 4.0 URI, matching frozen OAI rights and consistent article-license wording. The explicit permitted XLink href appears in 13 records; the other eight are ALI-only. Body screening found no unresolved rights notice in the included abstract/main-body scope. Four “adapted from” matches were methodological citations; two license mentions concerned software use; a CC0 keyword was a strain designation. A separate CC0 link in PMC5809949 is scoped to article data, which is excluded. Ten license/permissions nodes in PMC8262960 are confined to excluded supplementary material. The restricted-prose rights gate passes for 21 documents against a minimum of 20; zero corpus text was retained and no document was excluded.
- **Interpretation:** The SOURCE-007 protocol passes its rights gate for the frozen abstract/main-body prose scope. The result depends on recognizing the standard ALI URI pointer, consistent article-level CC BY 4.0 evidence, and the declared content exclusions; it does not clear figures, tables, captions, supplements, media, back matter, references, acknowledgements, permissions or non-prose markup. The SOURCE-005/006 explicit-href-only failure remains valid under those earlier protocols. JATS/NISO identifies `ali:license_ref` as a URI reference to license terms and includes it in the JATS `license` content model ([NISO element definition](https://jats.niso.org/1.1/Blue1_1/Blue1_1-elem-ali_license_ref.html), [NLM/JATS license content model](https://jats.nlm.nih.gov/tools/element-list/nlm_jats/el-license.html)).
- **Next experiment:** Q10-PREP-002: before preparing corpus text, freeze the deterministic abstract/main-body extractor and exclusions, document identity and normalized-duplicate policy, three-way train/validation/test split procedure and exact held-out document identities, and train-only tokenizer fitting and hashes. Prepare the frozen source twice and compare document, text, split and tokenizer hashes. Complete Q11 task contracts, scorer validation, contamination checks and fresh conventional baselines before any architecture training comparison.

## Q10-PREP-002 — Deterministic PMC prose preparation and train-only tokenizer fit (completed)

- **Status:** Preregistered at commit `63407f18b2dc4d13132cee9304d74149dcc2403d`; implementation completed on committed revision `c57f1860ad919920979607880d2db814fd15b39e`. Detailed hashes and held-out identity assignments are in [the Q10-PREP-002 result inventory](docs/q10i_pmc_preparation_result.json). Corpus text and tokenizer artifacts remain in ignored `data/`.
- **Hypothesis:** The 21 frozen articles can be extracted and split twice with identical source, text, duplicate, split and manifest hashes, while a deterministic byte-level BPE tokenizer fitted only on training documents yields identical artifacts and records no validation/test input.
- **Change:** Add a secure deterministic extractor for only the permitted primary abstract and article-body prose, with the SOURCE-007 exclusions. Use the existing schema-4 duplicate/split policy and canonical UTF-8/LF outputs. Fit `byte-bpe-v1` at a target vocabulary of 512 (including three special IDs), minimum adjacent-pair frequency 2, frequency-first with numeric-pair lexicographic tie-breaks. The tokenizer fit command accepts only one `train.txt` input; validation and test files are not available to it. Preserve the fit corpus hash and training identity hashes in the tokenizer artifact.
- **Control:** Q10-SOURCE-007's 21-document restricted-prose rights pass and Q10-PREP-001's schema-4 split/duplicate tests on synthetic fixtures. This is a reproducibility control, not a data-quality or capability control.
- **Training budget:** Zero additional network requests, model-training tokens, optimizer updates or training FLOPs. Tokenizer fitting is bounded CPU pair counting on the training split only. All raw XML, extracted prose, splits and tokenizer artifacts remain in ignored `data/` directories.
- **Evaluation metrics:** Exact XML hashes and identities; extracted document count and text hashes; normalized duplicate report; train/validation/test identity hashes; no duplicate leakage; A/B source/content/split/manifest/tokenizer-input/tokenizer-artifact hash equality; train-only tokenizer provenance; vocabulary and merge counts; Unicode round trips and UTF-8 byte coverage; zero model training.
- **Result:** Both independent preparations verified the same 21 frozen XML objects and produced the same 21-document corpus SHA-256 `1f237edeeb131455e90b611a9be653fb254f8241bc2c473b2b18bb8a3981b8a7` (593,275 UTF-8 bytes). Schema-4 outputs matched exactly: 17 train, two validation and two test documents; zero normalized duplicate groups. Train/validation/test SHA-256 values are `e5aac508fb9c0cb6e78bdbed7a3453174000f0b630f4ba9d0c312d37fcea77a3`, `47faec7d07370d8148e4228351ff49e0136cfe02ecdb0ffe64412fd1c61293f5` and `bfea9d78eb6b47e90ce9af367af62cac47f978e9d84a8f5b9ef16fc0147acdaa`. Both train-only fits made 253 merges, reached vocabulary 512, used 17 documents / 481,002 UTF-8 bytes and produced tokenizer SHA-256 `0570f7536d985403b6574b72f98355b1f12d0441641ff5e60cb1ede0eaa6bb33`. The exact PMCID.version mapping and every identity/hash are in the result inventory. All 21 documents round-tripped with exact UTF-8 byte coverage. Full suite: 113 passed; Ruff check and format passed. Model training tokens, updates and estimated FLOPs were zero.
- **Interpretation:** The frozen extraction, split and train-only tokenizer reproducibility gate passes. This is a small 21-article scientific-prose corpus; it does not establish general-English coverage, language-model capability or an efficiency improvement. Two documents per validation/test split are too few for strong generalization claims, so subsequent task and baseline results must remain descriptive and tightly scoped.
- **Next experiment:** Q11 task-contract and hand-checked scorer freeze, contamination checks against training/validation/test identities, then bounded fresh conventional-baseline results. Do not start the sequence-block campaign until those gates pass; keep the test set sealed until its one declared final evaluation.

## Q11-TASK-001 — Versioned synthetic instruction probe and scorer contract (completed; gates passed)

- **Status:** Preregistered on 23 September 2026 from clean parent revision `b7d46f55d3bb7bf119a1ea0d7d80e10f866c1f42`, after Q10-PREP-002 completed and before scorer changes or contamination scanning. The scorer and contamination protocol are frozen in [`docs/q11_task_contract_v1.json`](docs/q11_task_contract_v1.json); the four-example task file is [`configs/tasks/q11_synthetic_v1.jsonl`](configs/tasks/q11_synthetic_v1.jsonl), SHA-256 `b00cf0f6e599d3bd54600d3568e196a2628194ed7b21afebff4ea50cdd08bf6d`.
- **Hypothesis:** The four original toy examples have hand-verifiable targets, the scorer records exact-match and byte-normalized conditional likelihood per example, and no task string has exact identity or long-phrase overlap with Q10 train/validation/test content.
- **Change:** Freeze stable example IDs, exact prompt/target strings, the eight-token greedy generation cap, case-sensitive stripped exact match, conditional target NLL including EOS, and UTF-8 byte normalization with EOS assigned zero bytes. Freeze a mechanical full-identity plus casefolded 13-token overlap scan across all Q10 splits.
- **Control:** The hand-computed toy mappings and scorer truth table in the contract; existing `jsonl-prompt-target-v1` behavior is the compatibility reference. This is an evaluator-validation control, not a capability comparison.
- **Training budget:** Zero network requests, model-training tokens, optimizer updates or FLOPs. The test-set text may be scanned mechanically for contamination under the frozen rule; no model metric or task-selection decision may use it.
- **Evaluation metrics:** Task artifact hash and IDs; hand-checked targets; exact-match truth table; per-example scored tokens, UTF-8 bytes and bits/byte; deterministic repeated scorer output; exact identity and shared 13-token counts by split.
- **Result:** Hand-checked all four task targets and the four frozen exact-match truth-table cases. Scorer tests cover per-example UTF-8 byte accounting, EOS zero-byte semantics and deterministic audit behavior. Two clean CLI audit executions over Q10 A/B produced byte-identical 3,713-byte reports (SHA-256 `e52755947159e195c8bc93347e4248f345309b95151bef135daa5d76db2b6ba7`). Both passed the frozen task, contract, manifest, split and tokenizer hashes; scanned 21 documents (17/2/2); and found zero exact identities and zero shared 13-token n-grams across train, validation and test. Hashed task-string identities and per-example counts are in [`docs/q11_task_contamination_result_v1.json`](docs/q11_task_contamination_result_v1.json); no task or corpus prose is included. Full suite and style checks are recorded at the result commit.
- **Interpretation:** Q11.a's hand-check, byte scorer, reproducibility and frozen contamination gates pass. The screen does not establish semantic independence; four project-authored toy examples remain evaluator smoke probes, not a broad capability benchmark. Test content was used for the mechanical overlap audit only; no test model metric was calculated.
- **Next experiment:** Q11-BASELINE-001: preregister a bounded conventional GQA baseline with three fixed seeds on the frozen Q10 train data, validation set and tokenizer, estimate total compute before running, preserve every failure, and retain per-seed/per-example task results. Keep test model metrics sealed for the one declared final sequence-campaign evaluation.

## Q11-BASELINE-001 — Fresh Q10 conventional GQA control (completed)

- **Status:** Preregistered from clean parent revision `4fb40bfbef5ca2f9c4dfa2270d5e8c15aa62ac19`; all runs completed on clean revision `549ff8d26e12e529415efd7a8b858093965d0a48`. Frozen recipe and configs are in [`docs/q11_baseline_prereg_v1.json`](docs/q11_baseline_prereg_v1.json) and `configs/q11/`. The hash-only metrics, per-example task outputs, resource records and artifact checksums are in [`docs/q11_baseline_result_v1.json`](docs/q11_baseline_result_v1.json); sanitized run evidence is in [`results/q11-baseline-001`](results/q11-baseline-001/manifest.json).
- **Hypothesis:** A pinned conventional GQA recipe can complete the fixed 131,072-token budget on all three seeds and emit finite validation bits/byte and per-example task records with identical data, tokenizer, task, configuration family and source revision provenance.
- **Change:** Establish a fresh Q10 control with four dense full-attention GQA layers, width 128, context 128, 512-token byte-BPE vocabulary and the frozen PMC train split. Keep architecture, tokenizer, optimizer, schedule, data and evaluation fixed; vary only the initialization/data-sampling seed.
- **Control:** Three independent seeds (17, 42, 123) of the same conventional GQA recipe. This is a descriptive reference for later sequence-block work, not an architecture comparison or an improvement claim. M2-GQA-002 remains a separate synthetic TinyStories study.
- **Training budget:** 131,072 tokens and 256 optimizer updates per seed, batch 4 × context 128; 393,216 total consumed training tokens. `decoder-module-mac-v1` estimates 721,554,505,728 FLOPs per seed and 2,164,663,517,184 total under an equal total-compute cap. FLOPs are estimates and exclude scalar work, validation, task scoring and inference profiling. FP32 deterministic CPU training; no network requests. Validation and checkpointing occur at the final step and checkpoint intervals of 64.
- **Evaluation metrics:** Primary: full frozen validation conditional bits per UTF-8 byte, with two validation documents, context/stride 128 and document boundaries respected. Secondary: frozen Q11-v1 aggregate and per-example exact match and conditional bits/UTF-8-byte, plus per-seed consumed tokens, updates, estimated FLOPs, optimizer-active time, tokens/s, process memory and CPU prefill/decode latency, isolated peak RSS and cache bytes. No Q10 test model metrics.
- **Result:** All three seeds completed 131,072 tokens / 256 updates on CPU without failures. Validation conditional bits/byte were 3.70879, 3.69515 and 3.68912 (mean 3.69769, sample SD 0.01007) across two held-out documents. The four Q11 toy examples scored 0/4 exact match for each seed (0/12 total); task conditional bits/byte averaged 11.19413 (sample SD 0.16597). Total estimated training compute was 2,164,663,517,184 FLOPs. Training throughput averaged 21,618 tokens/s (sample SD 118); active training time averaged 6.063 s (sample SD 0.033) and run wall time averaged 7.615 s (sample SD 0.042). The 787,584-parameter model used 3,150,592 measured tensor bytes. CPU prefill/decode latency, isolated Win32 process high-water RSS, exact cache bytes and all per-seed artifact checksums are in the result inventory and portable bundle. No CUDA or energy reading was available; no Q10 test model metrics were computed.
- **Interpretation:** This establishes a fresh conventional Q10 control and a measured CPU workload reference. It is a negative baseline on the four toy generation examples and a descriptive language-model result on a small 21-document corpus; it is not a capability or efficiency improvement. Only two validation documents limit generalization and document-level uncertainty. All twelve task generations failed exact match. The test split remains sealed for model metrics.
- **Next experiment:** S02-STATE-001: make per-layer state and absolute positions explicit, then establish full/chunk/token parity, offsets and exact state-byte accounting. Only after correctness should the campaign add a dense local-attention reference and one recurrent/linear reference. No sequence-module training comparison starts until a fresh candidate is matched to this control at equivalent disclosed compute across at least three paired seeds.

## S02-STATE-001 — Explicit sequence state and absolute cache offsets

- **Status:** Correctness and same-checkpoint profiling completed on 23 September 2026. The implementation was developed from clean parent revision `64ce7ca884ba2553f660c131eac1fc30d2e39f83`; the pre-change fixed-input reference also ran from the separate clean clone at `790a83ac5c60b26ed0bd983d267ae28374488bad`. The machine-readable result is [`docs/s02_state_result_v1.json`](docs/s02_state_result_v1.json).
- **Hypothesis:** An explicit attention/recurrent state contract with absolute key offsets and recursive storage accounting can preserve conventional attention outputs and actual cache bytes while rejecting incompatible cache state.
- **Change:** Replace the implicit attention KV tuple with `AttentionState(key, value, key_start_position)` while preserving legacy iteration, indexing and tuple-cache acceptance. Add a `RecurrentState` container, deterministic nested tensor traversal, per-module cost/state descriptors, and profile schema 5 / state-accounting version 2. Profile prompts now use a local CPU generator seeded from the run seed and record prompt/parity input hashes. Validate cache position, shape, batch, dtype, device and offset at the model boundary; do not change parameter names or tensor shapes.
- **Control:** The same Q11-BASELINE-001 seed-17 `last.pt` checkpoint (SHA-256 `4917aba59bcf64d2a4d0fbd8c55cdf8ff7f5e55fe956fe723136ce435d92aab7`) and frozen config were used before and after. The pre-change implementation was also loaded from a separate clean clone; this is not the old Codex checkout.
- **Training budget:** Zero training tokens, optimizer updates, estimated training FLOPs, network requests or data preparation. The CPU profile used the existing 32/64/96-token prompts and eight incremental decode forwards; fixed-input parity used 17 tokens with chunk sizes 4/7/6 and one-token decoding. CUDA and energy were unavailable.
- **Evaluation metrics:** Same-checkpoint parameter and tensor bytes; allocated and active cache bytes per prompt; profile and accounting schema versions; exact full/chunk/token logits digests and maximum absolute errors; offset persistence; nested/shared storage-capacity versus active-region accounting; invalid-cache failure messages; checkpoint load success.
- **Result:** Before/after reprofile kept 787,584 parameters and 3,150,592 model tensor bytes. Allocated and active cache bytes after prompts of 32/64/96 plus eight decode forwards were identical: 81,920 / 147,456 / 212,992 bytes. The schema-5 profile used seed 17, recorded parity-input SHA-256 `e1638ea11e23fe0e7b2372104b5c882b96b5294cd93d881acc77fff9dd0fd9c1`, and measured full-vs-chunk maximum error `1.1920928955078125e-06`; schema 4 did not record its random input identity, so its matching numeric error is not treated as a paired scalar. State accounting is version 2. On the separately hashed fixed input, pre/post full-logit SHA-256 was `3c2e68c5e2c780c66b701f2af3e112262829515e56744c6cfa65d5b512bb4430`; chunked and tokenwise SHA-256 values were `b90de9de19647fa85c1c180d2f6a0f08410f08b604454d13d76982356e470c0c` and `71ad3367c76492157c506c77882a6d5af19d579c3c1ba4eeaf67a7368e27b3e0` in both implementations. Full/chunk and full/token maximum errors were each `1.9073486328125e-06`; allocated cache after 17 tokens remained 34,816 bytes. The Q11 checkpoint loaded without migration. Final validation: 123 tests passed in 28.89 s; Ruff check passed; 55 files were already formatted; `git diff --check` passed.
- **Interpretation:** S02 passes its CPU correctness and accounting gate. Offsets survive uneven cached chunks; nested shared storage is counted once; allocated capacity and active regions remain distinct; unknown opaque state stays unavailable. Profile inputs are now reproducible and identified by seed/hash. The measurement does not validate CUDA behavior, does not measure a speed improvement, and does not close Q08's hardware limitations or independently rerun M2-GQA-002.
- **Next experiment:** S03 dense local-attention correctness reference, beginning with fixed-weight tests and a slow causal-window oracle. Label the dense mask's executed work honestly; do not train until S03 correctness passes and a fresh matched three-seed plan is eligible.

## S03-LOCAL-001 — Dense local-attention reference correctness

- **Status:** Fixed-weight correctness and bounded-cache gates passed on 24 September 2026 from a working tree based on `fd3f2c462877d6a6c82d2207644a0ca007b7b06f` (the tree was dirty during validation); the exact tested changes were then committed as `e6d77c65fd3571d02fc6844ad026ae5ba3d406c2`. No learning or comparison run was started. Machine-readable record: [`docs/s03_local_attention_result_v1.json`](docs/s03_local_attention_result_v1.json).
- **Hypothesis:** A dense boolean causal-window mask can implement the declared local dependencies, preserve full/chunked outputs and gradients, and bound retained key/value state to at most `window_size - 1` prior tokens.
- **Change:** Add `local_attention_reference`, reusing the conventional MHA/GQA projections and RoPE. Define allowed keys by absolute positions `q - W + 1 <= k <= q`; evaluate every query against the combined prefix and current chunk before trimming. Copy only the final `min(W - 1, processed_tokens)` KV entries into contiguous cache tensors. Validate a positive module-specific `window_size`. Describe the path as `dense_masked_local_attention` and price it using the same full dense compute estimator as conventional attention.
- **Control:** `CausalSelfAttention` with identical weights when the local window covers the input, plus a slow per-query-slice oracle independently constructing each query's allowed key set. Gradient comparisons use identical inputs and scalar weights.
- **Training budget:** Zero training tokens, optimizer updates, estimated training FLOPs, data preparation or network requests. The fixed-weight checks use CPU tensors only.
- **Evaluation metrics:** Forward and input/parameter-gradient parity; W=1, W−1/W/W+1/2W+1 boundaries; odd chunks and a W-length first chunk; future and out-of-window influence; absolute cache offsets; MQA/GQA/MHA layouts; RoPE/learned positions; RMSNorm/LayerNorm; pre/post norm; invalid window configuration; actual retained state bytes; declared dense compute work.
- **Result:** Focused architecture/compute suite: 41 passed. Full suite: 149 passed in 29.25 seconds. Ruff check passed; 56 files passed the format check; `git diff --check` passed. The M2-GQA-002 portable bundle still verifies at SHA-256 `df79b3578c70c6fa7c3a0d0a2d34e02f257cec16c14295287d73912d2b9bef3a` with eight files. In the two-layer, batch-2, FP32 test model (two KV heads, head dimension 8), W=1 retained 0 bytes after both 1 and 9 processed tokens; W=4 retained 3,072 bytes after 3/4/5/9 tokens; W=20 retained 7,168 bytes after 7 tokens. These bytes were counted from the actual cache tensor storages and matched the declared bound exactly. Full commands, cases and limitations are in the result record.
- **Interpretation:** S03 passes the fixed-weight semantic and bounded-state gate. The boolean mask does not reduce executed attention work; the estimator intentionally charges the same dense score/value MACs as full attention. No latency, capability, or training-stability conclusion follows. CUDA behavior remains unvalidated. Q09-REPRO-004 subsequently reproduced the independent M2-GQA-002 comparator aggregates; that synthetic result does not evaluate S03.
- **Next experiment:** First complete the S06 normalized causal linear-attention correctness/resource reference at C0. Do not begin sequence comparison training until its invariants, toy dependency check, and CPU state/time behavior pass. If S04 is later selected, preregister one bounded local pilot with a fresh same-revision conventional GQA control and frozen Q10/Q11 train/validation evaluation. Keep Q10 test identities sealed; report all failures and do not claim improvement from a one-seed pilot.

## Q09-REPRO-004 — Independent full M2-GQA-002 replay (completed)

- **Status:** Completed 24 September 2026 at clean revision `dbab90d512d2b34e9738a6f6f8b62f33bd7c0eeb`. The replay was a reproducibility check, not a new model-selection opportunity. The machine-readable record is [`q09_repro004_full_rerun_result_v1.json`](docs/q09_repro004_full_rerun_result_v1.json); the verified portable bundle is [`results/q09-repro004-m2-gqa-002`](results/q09-repro004-m2-gqa-002/manifest.json).
- **Hypothesis:** On the reconstructed pinned TinyStories split and the documented Windows CPU environment, fresh MHA/GQA runs at the original three seeds will complete the frozen budgets and reproduce the recorded paired NLL/cache tradeoff closely enough to audit the original M2-GQA-002 result.
- **Change:** Execute the unchanged `configs/m2/tinystories_v3_mha_gqa002.json` and `tinystories_v3_gqa_gqa002.json` through `configs/m2/q09_repro004_m2_gqa002_sweep.json`. MHA config SHA-256 is `3bfbf0c64c8443afc105e3e327f3c430d8e7274c53faee372ecdd105e001d5dd`, GQA config SHA-256 is `43fb5ed8fc10c63f7dcec1e92cda61df02419c5d2360de59110c417a0293081c`, and sweep-spec SHA-256 is `de1bf75b4165d1b72b74a1d21e1812873eddeddbe2d5b6802094873df41fc003`. The spec preserves seeds 17/42/123, the 85T estimated-training-FLOP cap, and adds a 3,600-second cumulative runtime cap and a fresh output root. Frozen train, validation, prepared-manifest and tokenizer SHA-256 values are respectively `87ac33a8f47aeefdf772bd813472976eede4543d98d2c4122a1b07271163b44e`, `0ad8843aee6aff10a8bfc58fdfd80e26b261d061f341dffb12894a538f18a935`, `5bac1fc6d9ab7af5ed4c9a61d3a5ccbd2426faebb75acf2b605b013e146841ac`, and `7372690364e84d8c248945180d3c440742725dd79ac27f8f6bada3b7fd912687`. No training, evaluation, profiling, or comparison code changes are made.
- **Control:** Four-query/four-KV-head MHA, paired by seed with the two-KV-head GQA candidate. Both arms use the same schema-3 synthetic TinyStories train/validation split, `utf8-byte-v1` tokenizer, optimizer, context, evaluation and profile workloads. Each pair must share this run's clean source revision and environment.
- **Training budget:** Per seed, MHA: 2,097,152 tokens, 1,024 updates and 13,611,288,231,936 estimated FLOPs. GQA: 2,232,320 tokens, 1,090 updates and 13,610,794,352,640 estimated FLOPs (-0.003628%). Six-run total: 81,666,247,753,728 estimated FLOPs under an 85,000,000,000,000 compute cap and a 3,600-second cumulative runtime cap. FLOPs are estimates, not counters. The runtime cap includes the executor's elapsed wall time and may leave an explicitly partial sweep if reached.
- **Evaluation metrics:** Primary: full validation mean NLL in nats/token and paired GQA-minus-MHA deltas by seed, mean and sample SD; +0.02 nats/token is the preregistered practical decision threshold, not a significance test. Secondary: perplexity, UTF-8 bits/byte, parameter/tensor bytes, allocated/active cache bytes for prompt lengths 32/128/224 plus 32 decode forwards, prefill/decode and training throughput, wall time, process RSS and logit/cache parity. Record all run failures, compute consumption and CUDA/energy unavailability.
- **Result:** All six training jobs completed with no failures or omissions, using 81,666,247,753,728 estimated FLOPs in 1,864.850 seconds under the 85T and 3,600-second caps. Six post-training profiles completed separately in 79.348 seconds. The seeded comparator passed eligibility for the declared synthetic-corpus metric scope, and all seven aggregate comparator metric objects match the original portable bundle exactly. The paired GQA-minus-MHA NLL delta was -0.023073689 nats/token (sample SD 0.024392920, n=3); this is near the preregistered +0.02 practical threshold and is not a significance test. CPU cache state was halved at each profiled prompt length; GQA decode throughput was lower at all three lengths. CUDA and energy were unavailable. Full per-seed metrics and separately scoped storage, throughput, RSS, and parity measurements are in the linked result record. Do not replace the original result.
- **Interpretation:** The replay verifies bounded repeatability of this synthetic-domain comparison only. It cannot establish broad or human-authored capability, and three seeds remain a small uncertainty sample. Training process RSS is a process-lifetime high-water mark, not an isolated optimizer-step peak. The CPU decode measurements are workload-specific and show a latency cost alongside the reduced persistent KV storage.
- **Next experiment:** Start with S06's normalized causal linear-attention correctness and CPU resource-reference package, limited to a preregistered implementation, invariants, toy dependency check and measured state/latency behavior. Do not begin architecture-comparison training until that gate passes. Any later local-attention or hybrid comparison needs fresh same-revision conventional GQA runs on the frozen Q10/Q11 train/validation protocol; keep Q10 test model metrics sealed.

## S06-LINEAR-001 — Normalized causal linear-attention reference (preregistered)

- **Status:** Preregistered 24 September 2026 at clean revision `49c917125bb0d0e856c49fee59329b3833d7c67a`. This is a C0 implementation/correctness/resource package; it authorizes no corpus training or architecture comparison.
- **Hypothesis:** The normalized causal kernel formulation with `phi(x)=ELU(x)+1` can be implemented as a per-head recurrent reference with explicit key-value sum `S` and key-feature sum `z`. Its outputs should match an independent prefix-sum oracle, and its persistent inference state bytes should remain fixed as context length grows for a fixed batch/head shape. This is a correctness and resource-accounting hypothesis, not a latency or capability claim. Formulation: [Transformers are RNNs (Katharopoulos et al., 2020)](https://arxiv.org/abs/2006.16236), §§3.2–3.4.
- **Change:** Add `normalized_linear_attention_reference` with shared per-KV-head recurrent state for GQA, contiguous absolute-position validation, reset-by-`state=None`, and a Python token loop. Accumulate `S` and `z` in FP32; add a fixed `1e-6` denominator epsilon. Apply configured RoPE to projected Q/K before `ELU+1`; under learned positions, retain the model's existing learned embedding on hidden states. Require zero attention dropout because the recurrent reference has no attention-probability dropout implementation. Add an explicit recurrent MAC estimate and module state/cost descriptors. Do not add a parallel scan/kernel or change existing attention modules.
- **Control:** Independent slow prefix-sum calculation using the same projections and weights, with identical inputs. Test MHA/GQA dimensions, both configured position modes, full/chunk/token parity, forward/input/parameter gradients, no future leakage, absolute offsets, state reset, invalid state rejection, finite long-prefix values and actual state-storage accounting. Conventional softmax attention is not an output-equivalence control because the kernel and feature map differ.
- **Training budget:** Zero language-model tokens, corpus accesses, optimizer updates and comparison FLOPs. Profile a deterministic random-initialized four-layer GQA model with the Q11 model dimensions on CPU only: batch 1, prompts 32/64/96, eight cached decode forwards, one warmup and three timing repeats. Profile work is fixed-input measurement, not training. A learned toy dependency check is a separate follow-up package after the correctness/resource gate passes.
- **Evaluation metrics:** Oracle output and gradient errors; full/chunk/token max logit error; finite outputs/gradients; future-position perturbation effect; actual unique allocated and active state bytes after prefixes 1/32/64/96 plus eight decode forwards; CPU prefill/decode median, range and sample SD; isolated phase RSS high-water deltas; estimated recurrent MAC components and stated exclusions. Record no-CUDA and no-energy availability.
- **Result:** Pending implementation and C0 validation. Stop if the oracle, causality, state-bound, offset/reset, or gradient checks fail. Keep this result separate from Q09's synthetic TinyStories comparison and Q11's frozen Q10 baseline.
- **Interpretation:** A correct reference and context-independent persistent-state allocation would establish implementation semantics and the measured CPU behavior for this fixed shape only. The Python recurrence can still be slow and autograd may retain per-step training intermediates; constant inference state does not establish low training memory, useful retrieval, or an efficient kernel.
- **Next experiment:** If correctness, state accounting and the CPU resource profile pass, preregister a separate minimal synthetic dependency-learning fixture with a strict update/FLOP cap. Do not start local-attention, linear-attention or hybrid comparison training until the relevant reference and toy stability gates pass; any comparison requires fresh same-revision conventional GQA controls.
